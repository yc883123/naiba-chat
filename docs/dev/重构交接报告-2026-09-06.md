# Naiba Chat 模块化重构交接报告（2026-09-06·最终版）

> **快照基线：`master` HEAD `9c57b1d`（2026-09-06），83 个单测全绿；源码/冻结版双冒烟通过；全量功能回归（11 域 48 项）用户走查 ✅ 基本无大问题；版本已升级 `2.0.0-beta`，模块化重构（阶段 0–4）正式收官。**
>
> 本报告面向新会话窗口：自包含的「现状 + 已验证事实 + 守则 + 下一步」。权威方案见 `docs/dev/模块化重构设计方案.md`（§0.6 哲学指引与搬移守则、§3.5 契约落地记录）；功能回归清单见 `docs/dev/功能回归验收清单-2026-09-06.md`。

---

## 一、项目与重构目标（已达成）

- **Naiba Chat**：Windows 本机 AI 自动化工作台——本地 HTTP 服务（端口 8765）+ pywebview 桌面壳；对话、内置工具、后台 Job、Skill、MCP、视觉路由、ComfyUI、更新器、SQLite 存储都在一个进程里。
- **重构目标**：根目录约 20 个 Python 文件（原 `server.py` 5575 行等）迁入 `naiba/` 包（领域分层、单向依赖、组合根唯一），`server.py` 退化为门面；HTTP API 与事件协议**版式不动**；前端零配合。**已完成**。
- **哲学四支柱（§0.6）**：① 分层单向 DAG（三类边：import/运行期调用/契约）；② 边显式化（延迟导入清零、注入/Protocol，禁全局中转与字符串键）；③ 机械可验证（DAG/装饰器/残留引用/契约守门测试）；④ 结构≠行为（83 单测 + 每阶段编译实测护航）。

## 二、完成目标总览（阶段 0–4 全部 ✅）

| 阶段 | 内容 | 关键提交（本会话=059341e 之后的 19 个提交） |
|---|---|---|
| 0 | 缺陷修复 + 死代码清理 + 护栏 | （前一会话）fix 诊断开关、MCP 死代码、护栏 5 文件 |
| 1 | core 纯函数迁出 + 延迟导入清零 | （前一会话）依赖图 DAG |
| 2 | 运行时切分 + 契约 TypedDict 先行 | （前一会话）run/session·stream·manager·chat、tools、skills、storage、llm/vision、mcp/jobs/plans/subagent/search/capability/updater/net 归位 |
| 3.4.1 | 路径与配置 | `5e79698` paths（PathContext）→ `ad65748` config（ConfigStore 迁入，1598 行） |
| 3.4.2 | app 与 http | `e37129a` 迁移/网络/角色卡辅助归位 core → `ec50ef9` NaibaChatApp→`naiba/app.py` → `0ea933b` RequestHandler→`naiba/http.py` + AppHTTPServer 注入（launcher 参与） |
| 3.4.3 | 门面收口 + 业务下沉 | `c9897c2` server 收口 125 行门面 → `2bfae2b` 会话/工作区/运行设置五大块下沉 → `b170422` 16 个业务助手下沉 → `7c29745` Skill 安装三件套下沉（`_resolve_skill_dest` 契约改造） |
| 修复 | 搬移期缺陷 | `263eb2d` `_provider_profile`（@staticmethod+self 错配）→ `21d574d` `_upload`（self.app 残留引用）；均补守门测试 |
| 收尾 | 质量与契约 | `3065686` http.py 71 项无引用导入清理 → `7b4ca8f` 契约类型化收尾（AppContext/ConfigView Protocol、RunContext 校验工厂、MetadataKeys 写侧迁移） |
| 发布 | 版本与范围 | `edb7219` 回归清单 → `9c57b1d` 版本升级 2.0.0-beta + 权限矩阵守门 + 附录 C 范围收敛 |

**期间文档**：`ef55d66`/`09af066` 哲学⑥（两次事故复盘 + 3 条新守则）、`62803ad`/`9c57b1d` 交接报告迭代、`aeadd65` 设计方案完成状态、`edb7219` 回归清单。

## 三、当前代码布局（实测）

### 3.1 根目录

| 文件 | 状态 |
|---|---|
| `server.py`（**125 行**） | 门面：PC 与路径常量（8 个）、`APP: NaibaChatApp` 注解（launcher/main 赋值后使用，无先读后赋）、naiba 侧 re-export、三个薄委托（`write_status`/`acquire_instance_lock`/`main`→`naiba.http.main_entry`）、`__main__` |
| `launcher.py` | 仅一处改为 `srv.AppHTTPServer((host, port), srv.RequestHandler, srv.APP)` 注入；其余未动 |
| 14 个根 shim | `async_tasks/capability_runtime/job_registry/mcp_runtime/model_runtime/net_io/plan_runtime/skill_runtime/storage/subagent/tool_registry/updater/vision_runtime/web_search_runtime`（`from naiba.X import *`，自动收集进 PyInstaller） |
| `_cancel_regression.py` / `_smoke_harness.py` | 手工回归/桩工具（非 unittest） |

### 3.2 `naiba/` 包

```
naiba/
├── app.py        NaibaChatApp（组装根 + 全部 api_* 业务方法；_reply 约定返回 (payload, status)）
├── http.py       RequestHandler 薄包装 + AppHTTPServer（注入 app）+ 服务生命周期三函数
│                 write_status / acquire_instance_lock / main_entry
├── config.py     ConfigStore + default_config/built_in_agents/工具目录/provider 能力推断
├── paths.py      PathContext（八字段；default_path_context 冻结/源码双分支；static_asset_version lazy-once）
├── core/         contracts（RunContext/EventType/MetadataKeys/AppContext/ConfigView/校验工厂）
│                 choices paths diagnostics attachments file_changes conv_files history
│                 exceptions cards migration network
├── storage/      store.py(ChatStorage) media.py
├── run/          session stream manager chat（+ 根 shim: async_tasks）
├── tools/        registry.py（39 工具声明/分发） executor.py（执行+权限确认，权限矩阵）
├── skills/       agent.py catalog.py install.py policy.py context.py（exceptions 在 core）
├── llm/          runtime.py；vision/ runtime.py
└── 根级模块：net mcp jobs plans subagent search capability updater
```

### 3.3 关键契约与约定

- `core/contracts.py`：`RunContext`（TypedDict 18 键 + `RUN_CONTEXT_KEYS`/`default_run_context`/`validate_run_context`）、`EventType`（47 种，前端反查测试钉死）、`MetadataKeys`（读写侧全部常量）、`AppContext`/`ConfigView`（`@runtime_checkable` Protocol，ConfigStore/NaibaChatApp 真实实例即满足）。
- **业务下沉约定**：HTTP 层一律 `self._json(*self.app.xxx(body))`；app 方法 `_reply(payload, status) -> (payload, status)`；`_resolve_skill_dest` 契约 = `(结果, 错误应答元组)`。
- **注入**：`AppHTTPServer` 携带 app（`self.server.app`）；runtime 构造签名 `app: AppContext`（14 处已收窄）；`ConfigStore(path, paths=PathContext)`、`NaibaChatApp(paths)`。
- **门面面**：`server.py` 仅服务 launcher/tests（`RequestHandler/AppHTTPServer/NaibaChatApp/ConfigStore/APP/PC/路径常量×8/write_status/acquire_instance_lock/main`）；运行时永不 import server（DAG 守门）。

## 四、事故与守则（六次事故后的完整守则）

搬移守则七条见设计方案 §0.6⑤（三步验收：未定义名扫描→单测→双冒烟；定义集 diff；禁凭印象拼导入；整类搬移优先；每逻辑单元独立提交；提交卫生勿用 `-A` 且不提交 `build_info.json`/`.tmptest`；验证脚本写入 `.tmptest/*.py` 再执行并以 `PYTHONPATH` 双路径）。

§0.6⑥ 两条新守则（本会话二次事故补记）：
1. **装饰器-绑定错配是未定义名扫描的盲区**（`@staticmethod`+`self`）：搬移后必跑装饰器-绑定一致性检查（已固化 `test_static_decorators`）；
2. **`self.app.`→`self.` 替换的语法盲区**（`getattr(self.app, …)` 无点形式）：搬移后必跑残留引用扫描（已固化 `test_app_module_has_no_self_app_refs`）；附带：**停止命令返回 ≠ 端口已释放**，复测前确认旧进程退出（两次误判教训）。

## 五、验证资产与流程

### 5.1 测试（13 文件 83 用例）

| 文件 | 覆盖 |
|---|---|
| test_updater（17）/ test_workspace_binding（3） | 更新器 / 工作区绑定（原有） |
| test_choices / test_history_build / test_run_events / test_conv_files / test_config_migration | 选项识别 / 历史重放 / 事件流与取消竞态 / 文件边界 / 配置迁移 |
| test_dependency_dag | 无环 + naiba↛server + core 仅依赖叶子 |
| test_contracts | RunContext 键+校验工厂 / Protocol 实例化 / EventType vs 前端 / MetadataKeys |
| test_skill_catalog | SkillCatalog 扫描护栏 |
| test_tool_executor（2）/ test_tool_permissions（8） | confirm 流经 NEED_CONFIRM / 权限矩阵全模式（full/confirm/auto/deny、越界、MCP annotations、写工具双门槛） |
| test_static_decorators | @staticmethod 禁 self/cls + app.py 禁 self.app 残留 |

### 5.2 流程（环境纪律）

- **沙箱跑测试**：`$env:TEMP/$env:TMP=$env:PYTHONPATH=D:\naiba-chat\.tmptest`（需 `sitecustomize.py` 补丁，沙箱禁 chmod/mkdir(0o700)；**本机正常环境不需要**）。
- **未定义名扫描**：`.tmptest/scan_undef*.py`（复用 `scan_undef.collect_all_bindings`）。
- **冒烟**：源码 `python server.py`（后台 job）→ `/api/health` + `/api/bootstrap` + `/` + `/api/tool_catalog` + `POST /api/skills/scan` 全 200 → kill（确认进程退出后再复测）。
- **编译**：`$env:NAIBA_BUILD_VERSION="2.0.0-beta"; .venv\Scripts\python.exe -m PyInstaller --noconfirm --clean naiba-chat.spec`；产物 `dist\naiba-chat.exe`；编译前 `Get-Process naiba-chat | Stop-Process -Force`；`build_info.json` 滚动**勿提交**。
- 本机 **comfy-mcp 已连接**（health 返回工具清单）属环境常态。

## 六、下一步任务（按优先级，见设计方案 §0.5/附录）

| 优先级 | 任务 | 说明 / 门槛 |
|---|---|---|
| P0 | **2.0.0-beta 发布前最终编译** | 用 `NAIBA_BUILD_VERSION=2.0.0-beta` 重编 exe 并实测（版本号入 build_info 的最后一步） |
| P1 | **GitHub 发布** | 按 `版本更新说明.md` 执行剩余项：workflow 中 `RELEASE_VERSION/TAG/PACKAGE_NAME`、打 tag；workflow 自动覆盖 update.json 的 commit/sha256；发布后确认更新器校验链（manifest+SHA+exe 有效性） |
| P2 | **golden 录制-回放基线**（可选增强） | 以"83 单测 + 编译实测"为等效保障已是既定取舍；若为后续子模块立项做准备，补录「取消竞态/工具确认/多轮图片缓存」三类真实流量基线 |
| P3 | **清理项** | llm/vision/skills 包内细分（protocols/stream/router 纯函数化——改写风险高，等痛点出现再启动）；前端 app.js ES modules 化（阶段 5，移交决策）；`naiba/` 内部未使用导入审计（可选） |
| P4 | **另行立项（已移出本重构范围）** | 会话历史管理子系统、工具系统子系统——立项时以当前代码实际状态重做设计（交叉点已就位：core/history、run/session、MetadataKeys、tools/executor+权限护栏、app 组装收口）；立项门槛：golden 基线 + 契约已落地（均已就绪） |
| P5 | **版本路线** | 追踪 2.0.0-beta 实测反馈 → 2.0.0 稳定版规划（含前端模块化是否随行） |

## 七、技术雷区速查（保持有效）

- `server.py` 的 `APP` 是**注解**（launcher/main 赋值后才可读）；运行时模块禁止读它（已 Protocol 化）。
- `DATA_DIR` 二次重绑定语义在 `PathContext.rebind_data_dir`（同一对象，import 顺序无感知）。
- `STATIC_ASSET_VERSION` → `static_asset_version()`（lazy-once，禁止每请求重算）。
- `launcher.py` 依赖 `srv.AppHTTPServer/RequestHandler/APP/STATUS_PATH/RESOURCE_DIR/write_status/acquire_instance_lock/NaibaChatApp/main`，门面收口时必须保持可达。
- 冻结/源码双路径（`sys.frozen`/`_MEIPASS`/`%LOCALAPPDATA%\NaibaChat`）：新路径改动必出冻结版实测。
- `naiba/app.py` 禁 `self.app` 引用（守门测试），`http.py` 中 `self.app` 是注入访问器（合法）。
- 历史测试源码从未入库（仅 pyc 残影）不可恢复；`_cancel_regression.py` 是独立手工回归。
- `develop` 为另一贡献者分支，不拉取；主线保持当前 master。

## 八、参考文档

- `docs/dev/模块化重构设计方案.md` —— 权威方案（哲学/守则/阶段状态/契约落地/附录）
- `docs/dev/功能回归验收清单-2026-09-06.md` —— 全量回归清单（11 域 48 项 + 记录）
- `docs/dev/重构交接报告-2026-09-06.md` —— 本报告
- `版本更新说明.md` —— 发布检查清单（示例与执行记录已更新为 2.0.0-beta）
