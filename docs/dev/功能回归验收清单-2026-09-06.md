# Naiba Chat 全量功能回归验收清单（2026-09-06）

> **适用**：重构（阶段 0-4）收官后的全量人工验收。自动化已覆盖项见 §A 预检状态，无需重复。
> **用法**：逐项走查，结果栏填 ☑/☒；☒ 项附现象截图或文字。**「重构敏感」列 = 该路径被本次重构改动直接触及**，属重点。
> 建议顺序：先做「重构敏感」项，再做全量。

## §A 自动化预检状态（已通过，无需重复）

| 项 | 结果 |
|---|---|
| `python -m unittest discover -s tests`（75 用例：DAG 校验/契约/装饰器守门/残留引用守门/护栏） | ✅ |
| 源码冒烟 `/api/health` `/api/bootstrap` `/` `/api/tool_catalog` + `POST /api/skills/scan` | ✅ |
| 冻结版编译（`dist\naiba-chat.exe`，commit `aeadd65`）与抽样走查 | ✅ |
| 静态检查：未定义名扫描、装饰器-绑定、`self.app` 残留、DAG（naiba↛server） | ✅ |

## §B 启动与生命周期

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| B1 | 桌面启动 | 双击 exe | 窗口+托盘出现，无报错弹窗 | ✅（AppHTTPServer 注入链） |
| B2 | 单实例锁 | 启动后再启动一个 | 提示"已经在运行" | ✅（acquire_instance_lock 迁 http.py） |
| B3 | 托盘退出/关窗 | 关窗口→点托盘退出 | 退出后 `%LOCALAPPDATA%\NaibaChat\server.json` 被清理 | ✅ |
| B4 | 首启数据目录 | 全新环境（无配置）启动 | 写入 `%LOCALAPPDATA%\NaibaChat\`（非 exe 相邻目录） | ✅（PathContext） |
| B5 | 旧数据迁移 | 模拟 exe 相邻旧 config/data | 启动自动迁移且不覆盖新数据 | ✅（migration.py 参数化） |
| B6 | 自定义 data_dir | 设置→数据目录→切换→重启 | 切换生效；旧目录数据不动；重启后指向新目录 | ✅（rebind 语义） |

## §C 会话管理

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| C1 | 新建会话 | 新建（含指定 Agent/权限/思考强度/工作区分组） | 创建成功；工作区目录=分组注册目录 | ✅（api_create_conversation） |
| C2 | 会话列表 | 新建多会话、折叠/展开/拖宽侧栏 | 虚拟化流畅、无重复 | — |
| C3 | 删除会话 | 删当前/非当前 | 非当前不打断流式；当前切下一条 | — |
| C4 | 会话设置保存 | 改标题/系统提示/模型/Agent/权限/轻量/富文本 | 全部生效；Agent 切换在工具集固化后拒绝(409 提示) | ✅（api_update_conversation_settings） |
| C5 | 工作区切换 | 会话设置→选分组/未分组 | 分组=目录随组切换；未分组保留原目录 | ✅（下沉后语义保留） |
| C6 | 分支/重编辑 | 消息菜单→从此重编辑 →改文重发 | 截断后重发成功 | ✅（api 链路） |
| C7 | 快捷提示词 | 设置→对话快捷提示词 增删改查 + 对话设置选择预设 | 预设填充；编辑不影响已有会话 | — |

## §D 对话流

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| D1 | 流式回复 | 发普通问题 | delta 流式、状态行、思考块顺序正确 | — |
| D2 | DeepSeek 思考 | 开思考模型 | reasoning_content 展示、不发工具 | — |
| D3 | 富文本 | 开/关富文本开关 | 白名单标签渲染/纯文本，切换即时重渲染 | — |
| D4 | 选项按钮 | 让模型给出"请选择：1.… 2.…" | 按钮出现；点按只发选项文本；规则条隐藏 | ✅（choices 迁 core） |
| D5 | 代码/表格 | 回复含代码块+表格 | 流式同步高亮/表格；多个代码块不误判工具调用 | — |
| D6 | 轻量模式 | 单独关工具/Skill | 对应能力关闭；富文本开关独立不受影响 | — |
| D7 | 取消/中断 | 长回复中点停止 | 立即中断；已输出部分保留为"未完成"；可续发不卡死 | ✅（事件流重建） |
| D8 | 断线重连 | 流式中刷新页面 | 复用气泡继续（after 序号续传） | — |

## §E 工具与权限

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| E1 | 工具确认 | 让模型执行 pwsh/write_file | 确认弹窗（工具/参数/原因）；确认/拒绝生效 | ✅（ToolExecutor 归位 tools/executor） |
| E2 | 权限模式 | confirm/auto/full 各测一次 | 行为符合模式定义（高危仍确认） | — |
| E3 | 文件工具 | 工作区内读写/搜索/编辑 | 成功；越界路径拒绝 | — |
| E4 | Agent 工具目录 | 编辑页：分类/预设/全选 | 目录与预设正确；"联动工具须同时选中"提示 | ✅（工具目录迁 config） |
| E5 | 工具集固化 | 首条消息后修改 Agent 工具 | 会话内不可变；新开会话生效 | — |
| E6 | 高风险拒绝 | 拒绝一次确认 | 模型获得失败说明并继续，不阻塞 | — |

## §F Skill 系统

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| F1 | Skill 索引与引用 | 输入 `/`，选 Skill | 索引出现；发送后注入生效（首轮 system） | ✅（skills/agent 整类搬移） |
| F2 | 安装 Skill（zip） | 设置→安装 Skill（zip 内含 SKILL.md） | 安装成功、进入列表、导入即启用（隐藏项自动取消隐藏） | ✅（install 三件套下沉 app） |
| F3 | 安装文件夹/单 md | 同 F2 两种形态 | 成功 | ✅ |
| F4 | 删除/隐藏 | 删除（进回收目录）；隐藏 | 删除后引用清理、Agent 固定 Skill 同步；隐藏可恢复 | ✅（delete 族下沉 app） |
| F5 | Skill 脚本 | 触发 run_skill_script（含冻结版） | 脚本正常执行 | — |
| F6 | 安装目录管理 | 添加/移除自定义 skill 目录 | 扫描范围更新；越界/系统目录拒绝 | ✅ |

## §G MCP / ComfyUI / 视觉 / 搜索

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| G1 | MCP 注册/测试 | 设置→连接→添加 stdio 服务→测试连接 | 测试返回真实状态（连接/错误） | ✅（MCPRegistry.connection() 公开访问器） |
| G2 | MCP 工具进会话 | 注册后重开会话，工具页勾选 | 会话可用集含 mcp__ 工具；call_mcp 可调 | — |
| G3 | Comfy MCP | 本机 comfy-mcp | health/bootstrap 返回其工具；comfy 工作流批处理可用 | ✅ |
| G4 | ComfyUI 批处理 | comfyui_prepare_workflow + comfyui_batch | 校验/负 seed 修正/批量提交/产物收集缩略图 | — |
| G5 | 视觉路由 | 文本模型+配置视觉后端；发图询问 | 自动路由调用后端；预览正常 | ✅（vision 整模块归位） |
| G6 | 多模态直读 | 多模态模型 + vision_read_folder | 图片注入消息；缓存命中率正常增长 | — |
| G7 | web_search | 配置搜索引擎→对话触发 | 搜索结果引用 | — |
| G8 | recall_history | 让模型检索历史会话 | 返回按会话聚合的命中 | — |

## §H 上传 / 文件面板

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| H1 | 上传文件/图片 | 聊天输入区上传 | 成功；图片缩略图；超 80MB 拒绝 | ✅（_upload 下沉 app，曾 500，已修） |
| H2 | 文件面板打开 | 点 AI 文件变更摘要→面板 | 打开最近文件；标签页保留 | — |
| H3 | 文本编辑保存 | 面板编辑→Ctrl+S | 写回磁盘；越界/未改动文件拒绝保存 | ✅（conv_files 迁 core） |
| H4 | 越界访问 | 尝试打开工作区外路径（构造 URL） | 拒绝（无权访问/保存） | ✅ |

## §I 后台 Job / Plan / 子 Agent

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| I1 | run_in_background | 让模型起后台任务 | Job 创建；job_status/wait/output 可用；可取消级联 | ✅（jobs 归位） |
| I2 | 任务弹窗 | 打开任务弹窗 | 状态/日志流式；清理终态 | — |
| I3 | Plan 模式 | 新会话选 plan 模式 → 计划 → 执行 | prepare→ready→building→finished；步骤可见；可编辑/keep-planning | ✅（plans 归位） |
| I4 | 子 Agent | 让模型委派 subagent | 深度/数量限制生效（深度>2 拒绝） | — |

## §J 设置页全域

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| J1 | 供应商 CRUD/测试 | 新增/编辑/删除；测试连接（含**不带 model_key 的内联路径**） | 成功/真实错误；无 NameError | ✅（两次修复重点） |
| J2 | 模型档案/默认模型 | 档案增删改、设置默认 | 生效；能力推断显示 supports_images/上下文 | ✅ |
| J3 | 卸载本地模型 | 卸载 ollama/lmstudio 模型 | 释放 | — |
| J4 | 代理设置 | enabled/url/系统回退 三态切换+保存 | 即时生效；设置页显示实际生效模式与说明 | ✅（net_io 收口） |
| J5 | 网络访问状态 | bootstrap/设置页 | LAN URL/口令显示正确 | — |
| J6 | 备份与迁移 | backup/run/move-data/merge | 各操作成功；有活动任务时拒绝 | ✅（migration 族归位 core） |
| J7 | 图片缓存清理 | 设置→缓存清理 | 清理成功、显示释放量 | — |
| J8 | 访问口令/端口 | 修改 host/port/token | 提示重启生效（restart_required） | ✅（settings 编排下沉 app） |

## §K 其他

| # | 验收项 | 步骤 | 预期 | 重构敏感 |
|---|---|---|---|---|
| K1 | 更新器 | 检查更新（限流时走缓存/latest 兜底） | 正常显示/可选版本；安装校验 manifest+SHA | — |
| K2 | 移动端布局 | 窄窗口 | 无横向滚动；选择器网格/输入区正常 | — |
| K3 | 鉴权 | 局域网访问口令 | 无口令被拒（401）；本机放行 | — |
| K4 | 插话控件 | 界面 | 已移除（无残留入口） | ✅ |

## 回归记录

| 日期 | 范围 | 结果 | 备注 |
|---|---|---|---|
| 2026-09-06 | 抽样（对话/设置/上传/契约收尾 exe） | ✅ | 用户走查通过 |
| 2026-09-06 | **全量（11 域 48 项）** | ✅ | 用户走查通过，基本无大问题；「权限矩阵/选项检测/契约/DAG/残留引用」等自动化守门项无需人工重复（§A） |
