# 2026-09-02 工作日志

## 上午：发布 1.6.6-beta（按 .codebuddy 版本更新规则）

- 本次改动主题：**收紧 AI 交互选项识别**（server.py + public/app.js）：
  - `_detect_choice_groups` 重构：新增 `MAX_CHOICE_LEN=40` / `MAX_CUE_LEN=40` / `MAX_CUE_DISTANCE=2` 阈值；意图行必须紧跟选项组（≤2 行）否则视为普通列表；选项/意图文本过长不算；英文只保留完整词组（pick one/choose one/select one/which one/which of/choose from/select from），裸 select/choose/pick 不再误判；移除 named 命名选项兜底；CJK 标点后也能拆分紧凑选项（「请选择语言：1. 中文 2. 英文」）；剥离选项编号残留；孤立 bullet 不打断选项组
  - public/app.js `showChoiceButtons`：`formatAnswer` 改为只返回选项文本，不再拼接 prompt 前缀
- 版本文件更新为 1.6.6-beta：release.yml（版本/tag/包名/发布名/body）、README.md、naiba-chat-update.json、release_notes.json（JSON 校验通过）
- 提交 `27b00f4`（`feat: tighten choice option detection and release 1.6.6-beta`），push master 触发 Actions run #129 成功，Release `v1.6.6-beta` 资产完整（exe 36.2MB / zip 35.9MB / update.json）
- 校验：JSON 合法、node --check 通过、server.py ast 解析通过；残留的 1.6.5 仅存于历史 release_notes（符合规则）
- 注意：本次只提交 6 个文件（release.yml、README.md、naiba-chat-update.json、release_notes.json、server.py、public/app.js）；untracked 的 `deepseek-harness/`、`dsh-vision-router/`、`docs/`、`scripts/`、`_cancel_regression.py`、`.workbuddy/`、skills 下若干文件仍**未提交**

## 上午：修复"删除旧会话导致新会话流式输出消失"bug（未提交）

- 根因：`public/app.js` `deleteConversation(id)` 删除**非当前**会话时也无条件 `await openConversation(state.conversations[0].id)`。分支场景下 `conversations[0]` 正是正在流式的当前会话 B，`openConversation(B)` 因 `id===state.conversationId` 不 detach，但执行 `renderMessages()` 重建消息列表，把流式 row 挤出 DOM → 流式事件仍写入已脱离 DOM 的 `state.runRow`，直到 run 结束 `finishRunSubscription` 重新渲染才"重现"
- 修复：删除非当前会话时只 `renderSidebar()` 并直接 return，不触碰当前会话视图；删除当前会话才走 `openConversation` 切换。`node --check` 通过

## 上午：发布 1.6.7-beta（按 .codebuddy 版本更新规则）

- 本次改动 = 上面的删除会话流式修复（public/app.js `deleteConversation`：`wasCurrent` 判定，非当前会话只刷新侧栏 return，不重载当前视图）
- 版本文件更新为 1.6.7-beta：release.yml（版本/tag/包名/发布名/body）、README.md、naiba-chat-update.json、release_notes.json（JSON 校验通过）
- 提交 `79ddfdd`（`fix: keep current stream alive when deleting non-current conversation and release 1.6.7-beta`），push master 触发 Actions run #130 成功，Release `v1.6.7-beta` 资产完整（exe 36.2MB / zip 35.9MB / update.json）
- 校验：JSON 合法、node --check 通过；残留的 1.6.6 仅存于历史 release_notes（符合规则）
- 经验：查最新 Release 用 `/releases/tags/vX.Y.Z-beta` 或 `/releases?per_page=1&page=2` 更可靠，`/releases?per_page=1` 可能命中 WebFetch 15 分钟缓存返回旧数据
- 注意：本次只提交 5 个文件（release.yml、README.md、naiba-chat-update.json、release_notes.json、public/app.js）；untracked 的 `deepseek-harness/`、`dsh-vision-router/`、`docs/`、`scripts/`、`_cancel_regression.py`、`.workbuddy/`、skills 下若干文件仍**未提交**

## 中午：server.py 模块化拆分（develop 分支，未提交前先记住）

- 目标：447KB/4693 行的 server.py 拆成聚焦子模块，纯"搬移不重写"
- 新增 6 个模块（累计搬出 ~2430 行，server.py 降到 2406 行）：
  - `app_state.py`（61 行）：EXE_DIR/APP_DIR/DATA_DIR/STATUS_PATH/LOCK_PATH/CONFIG_PATH/PUBLIC_DIR + 可变全局 APP。**关键设计**：DATA_DIR/STATUS_PATH/LOCK_PATH/APP 会被 NaibaChatApp.__init__ / main 重绑定，必须经 app_state 同步（__init__ 末尾 `app_state.DATA_DIR=...`、`app_state.APP=self`），否则拆出的子模块读到旧值
  - `image_utils.py`（332 行）：图片处理/缩略图/缓存清理/媒体类型/encode_image_for_model
  - `config_helpers.py`（253 行）：default_config、旧数据迁移、模型常量（ONLINE/LOCAL_REQUEST_FORMATS、VALID_MODEL_KINDS 等）
  - `agent_catalog.py`（151 行）：built_in_agents / tool_catalog_entries / 工具分组常量
  - `model_media.py`（659 行）：build_model_history / extract_attachments / _detect_choice_groups / _image_intent / 能力推断
  - `config_store.py`（974 行）：ConfigStore 类
- server.py 保留：NaibaChatApp、RequestHandler、LAN 工具（_is_usable_lan_ipv4/get_lan_ip/network_access_status）、write_status/acquire_instance_lock/main，顶部 import 全部新模块 re-export（async_tasks/plan_runtime/subagent/skill_runtime/model_runtime/vision_runtime 的懒加载 `from server import ...` 与 launcher `srv.*` 全部兼容，无需改）
- 验证：py_compile、import server/launcher、AST 未定义名扫描（修复了 3 处：LAN 块漏搬、MODEL_IMAGE_HISTORY_LIMIT 未导、image_utils 残留裸 APP）、_smoke_harness 全 PASS、8 项功能点（ConfigStore 读写/选项检测/历史构建/PIL 缩略图/能力推断/图片意图/全局同步/工具目录）、隔离目录真实启动（自定义 data_dir + app_state.APP 同步）
- 注意：git diff 显示 LAN 块"没算新增"是 Myers 算法位置无关的渲染现象，实际内容完整
- 12:57 已推送：`git push -u origin develop` → GitHub 新建 origin/develop（yc883123/naiba-chat），master 与发布构建不受影响

## AskUserQuestion 逆向调研（18:10）
- 回答用户「AskUserQuestion 有无源码 / 与 Naiba 前端谁好」：源码闭源但可逆向，打包于 `D:\workbuddy\resources\app.asar`。
- 定位方法（可复用）：`@electron/asar` listPackage 返回 Windows 反斜杠路径；extractFile 需去掉前导反斜杠保留内部反斜杠（`f.replace(/^\/,'')`）。命中 3 文件：`\cli\dist\codebuddy.js`(主进程,74 处)、codebuddy-headless.js、`\cli\dist\web-ui\assets\index-rVc9k2Jj.js`(前端,1.7MB minified)。
- 机制还原：AskUserQuestion=BaseTool 子类（zod schema：questions 1-4、question 必填、header≤12 字、options 2-4 含 label+description 且唯一、multiSelect、answers record<string,string>、metadata.source）；needsApproval 恒真→interruptionService+askService.setAsk→ACP `_codebuddy.ai/question`（schema:{questions:[{id:q_i,...}]}）→前端 store addQuestion/submitQuestion/cancelQuestion（timeline 增 pending 节点，阻塞 agent）→BB/$B/UB/zB 组件渲染（多题分页 tab、单选圆点/多选方块、每题下"或输入自定义答案"输入框、Review 汇总、Submit/Cancel、answered/cancelled/invalid 三种终态）；答案以 q_0… 键回传，取消返回 "User declined to answer questions"；还有 skip_question/skip_interview 的 _action 快捷分支。
- Naiba Chat 现状对照：已有同构地基——permission_mode confirm/auto/full、`/api/tool/confirm|reject`(confirm_id)、plan bar（ready 的 Approve/Edit/Keep planning）、消息内 tool 卡片"允许执行"按钮；缺的是"结构化多选题工具通道"（AI 提问目前靠 prepare 文案让用户直接打字 + 原生 confirm）。若移植：后端加 ask_question 工具（confirm_id 通道复用）+ 前端加 question 消息类型卡片即可，无需抄 React。

## Naiba Chat ask_user 结构化提问：勘察完成 + 方案文档（18:40）
- 用户选择"先出完整方案文档"（未动源码；master 有用户未提交改动，未触碰）。
- 产物：`docs/dev/ask_user_实现方案.md`（新目录 docs/dev）。
- 关键勘察结论（master 分支现状）：
  - 3 种现有人机交互：① 工具确认 confirm（ToolExecutor.execute→`NEED_CONFIRM:<id>:<reason>:<args>` → skill_runtime `_execute_with_retry`(~2037) 发 tool_confirm 事件 → wait_for_confirmation 轮询 confirmation_results 5 分钟 → /api/tool/confirm|reject）；② choice 文本选项检测（server.py:484 `_detect_choice_groups`，非阻塞、答案填输入框 fillComposer）；③ plan bar。
  - 分发链：tool_registry.execute（spec 存在→executor.execute）→ Craft/ReadOnlyToolExecutor 包装（均放行非特判工具）→ ToolExecutor.execute（skill_runtime:568，confirm 判定前）→ 返回 NEED_* 字符串。RunManager 在 async_tasks.py（emit ~750 / owns_confirmation ~1731 / executor_for_run；取消清理 ~1686）。server.py _confirm_tool ~4796、路由 ~4112。模型 tools schema：tool_registry.schemas() → async_tasks ~1003 按 allowed_tools 过滤（tools 数组须常驻，防 DeepSeek 前缀缓存漂移）→ _tool_schemas(model_runtime:953)。ALL_TOOLS 在 plan_runtime.py:29，READONLY_TOOLS :40，resolve_mode_tools :68。
  - 方案核心：新增 ask_user 工具（契约照 WorkBuddy AskUserQuestion 逆向：questions 1-4、header≤12、options 2-4 唯一、multiSelect、UI 自带自定义输入勿给 Other、任意答案有效）；ToolExecutor 加平行 pending_question/question_results + answer/skip/wait 三方法；事件加 tool_question；API /api/tool/answer、/api/tool/answer_skip；前端 handleRunEvent 加 renderQuestionCard（全题纵排，单选圆点/多选方框/自定义输入/提交跳过/answered 摘要）。
  - 决策点留给用户：R1 auto/full 下 ask_user 照常弹窗；R2 plan 只读模式是否放行 ask_user（建议放行，澄清最常用）；R3 tools 常驻防缓存漂移。

## 傍晚：左右侧栏折叠 / 展开（用户中断一次再次确认"折叠和展开"）
- 前端三点：
  - 左侧栏：`#collapseSidebar` 在 `.sidebar-top`（深色 chevron `«`），桌面默认显示、≤760px `display:none`；点击 → `appShell.classList.add('sidebar-collapsed')` → `--sidebar-col: 0px` 且 `.sidebar { visibility:hidden; overflow:hidden; pointer-events:none; }`，主区铺满整宽。状态存 `localStorage.naibaChatSidebarCollapsed='1'`；`#expandSidebar`（顶栏最左 chevron `»`）默认 `display:none`，仅 `.app-shell.sidebar-collapsed` 时显示，点击清除类与 localStorage 键。
  - 右侧文件面板：原 `#closeFilePanel` 标题改为"收起文件面板（Esc）"；顶栏新增 `#openFileTabs`（`hidden` 状态，桌面 + 有标签 + 面板当前未打开 三者皆 true 才显示），count 来自 `filePanelState.tabs.length`，点击 → `reopenFilePanel()` 用 `activeKey` 恢复面板。逻辑统一走 `updateFileTabsButton()`，挂在 `renderFilePanel()` 末尾和 resize 监听里。
  - 手机端：折叠类永不设置（`sidebarDesktop()` 守卫 + resize 移除），沿用原有抽屉（☰/×）。`#expandSidebar` 由 `display:none` 默认控制，CSS 媒体查询里不额外处理。
- CSS：`.app-shell { --sidebar-col: var(--sidebar-w, 272px); grid-template-columns: var(--sidebar-col) minmax(0,1fr); }`；`.app-shell.file-panel-open` 把第一轨改写为 `var(--sidebar-col)` 以便折叠后自动为 0；`#expandSidebar` 默认 `display:none`、`.app-shell.sidebar-collapsed #expandSidebar { display: inline-grid }`。
- 验证：隔离实例 8787 + Chrome CDP `scripts/regression-collapse.mjs`（网格字符串/类名/localStorage 全部断言通过）：左收 0px 整宽、右收标签保留 count=1 重开、左右同收 0px/主区/430px 三轨。
- 踩坑：**并行 Edit 同一文件**会丢更新（一个 tool 块里两个 Edit 写同一文件，其中一个的目标 old_string 在并发读到的快照里成立、但另一个写完后被覆盖）。本次丢的是 ① 左侧栏 collapse 按钮、② openFileTabs 按钮、③ `.app-shell` 基线 `--sidebar-col` 默认定义、④ 早期 file-panel-open grid 重写也踩过。后通过 `grep` 落盘校验 + 重发单条 Edit 修复。**规则：同一文件的多处编辑必须拆成多次单条 Edit 工具调用**。
- 真实截图（1080p 桌面板本/视觉证据）：`_iso_test/shots/collapse-left-folded.png`、`collapse-right-folded.png`、`collapse-both-open-right.png`。

## 晚上：#6 整体验证收尾（虚拟化滚动 + 手机端总结）
- 后端：`_iso_test/` 内对全部 17 个 *.py `py_compile` 全 PASS，`import server` 成功导出 139 个名字；`_smoke_harness.py` 6 项（tool_registry/job_shell/agent_final/agent_maxsteps/agent_retry）ALL SMOKE TESTS PASSED。
- 前端虚拟化修复回归（`scripts/regression-virtualize.mjs`，1600×1000 + headless Chrome CDP）：
  - 展开 1 个工作区后 26 条会话；`scrollHeight=1984, clientHeight=832, maxScroll=1152` → 虚拟化必须工作。
  - 故意 `scrollTop = 9999999` → 渲染线程回写到 `scrollTop=1136`（≈ maxScroll-SIDE_BUFFER），并把 `.sidebar-virtual-window` 顶位移到 `top:860px`，可见区无空白。**clamp 修复有效**。
  - 滚到底部、点击最末一行（历史会话 01）：会话成功打开、`msgCount=4`，scrollTop 仍在合法范围（1072 ≤ 1152）。视觉截图 `virt-desktop-overscroll-clamped.png` 可直接看到 20 条连续会话整齐排列。
  - 反向回滚到顶部并点首行：active 类正确加到 `19e395b5…`，scrollTop=0。
- 手机端快照（375×812）：
  - `.file-change-chip` `pointer-events: none` + `background: rgba(0,0,0,0)` + `border: 0px`（彻底变纯文字 "新 rel.txt 新 note.md …"），满足"仅总结、不可点"。
  - `#filePanel { display: none }`、顶栏 `#openFileTabs` `hidden=true`、左右折叠按钮 `display: none`——手机端走原抽屉逻辑。
- 截图（手机端）：`_iso_test/shots/virt-mobile-summary.png`。
- 任务 1–6 全部 completed。

## 晚间补丁：中间栏宽度随左右侧栏开合响应

- 用户反馈：左栏折叠/展开看起来不影响中间那一块。根因——`grid-template-columns` 折叠时确实把 main 从 1270→1440px（CDP 实测），但 `.messages` 与 `.composer-wrap` 用固定 `880px` 居中限宽（`padding: ... max(22px, calc((100% - 880px) / 2)) ...`），多出的宽度全变成两侧留白，视觉上像没变。
- 修复（纯 CSS，复用既有 `.sidebar-collapsed` 状态，不改 JS）：
  - `.app-shell` 引入 `--chat-max: 880px;` 默认值。
  - `.app-shell.sidebar-collapsed { --chat-max: min(1400px, calc(100% - 240px)); }`。
  - `.messages` 与 `.composer-wrap` 把公式里 880px 改为 `var(--chat-max)`。
- 实测（1440 视口）：展开 865px 内容列 / 195px 边距；折叠 1185px / 120px 边距（+37%）。截图 `_iso_test/shots/chatmax-expanded.png`、`chatmax-collapsed.png`。
- 同一文件多 Edit 仍要分次单条调用（这次先有教训，已自动拆开，没再丢更新）。
- 8700+ 字符的本日日志仍可写，3000 字 MEMORY.md 限额未触碰。

## 晚间补丁 2：右侧文件面板入口常驻

- 用户反馈："右边侧边栏怎么现在没按钮"。根因：`#openFileTabs`（顶栏「文件 N」按钮）旧的显示条件是 `hasTabs && !open`——会话没点过任何文件 chip 时 tabs 为空 → 按钮 `hidden`，右面板像消失了一样。
- 修复：
  - `updateFileTabsButton` 改为 `show = filePanelUsable() && !filePanelState.open`（面板关闭即常驻），无 tabs 时 `#fileTabsCount` 设 `hidden=true` 隐藏空徽标；title 动态化。
  - `reopenFilePanel` 去掉"无 tabs → toast"分支，无 tabs 也直接打开面板（空态已有 `file-panel-hint` 引导文案）。
  - `styles.css` 加 `.file-reopen-button #fileTabsCount[hidden] { display: none; }`（UA 的 `[hidden]` 规则会被作者样式 `display:inline-grid` 覆盖，必须显式补一条）。
- 截图（1440 桌面）：`_iso_test/shots/reopen-btn-empty.png`（顶栏空态「文件」按钮）、`reopen-panel-empty.png`（点击后空面板 + 引导文案）。

## 重大修复：前端改动浏览器看不到 = asset 版本号启动时固定（20:38）
- 现象：改了一整天 app.js/styles.css，8765 用户浏览器怎么刷都是旧 UI。
- 根因：`server.py` `STATIC_ASSET_VERSION = static_asset_version()` 是**模块级常量、进程启动时算一次**。8765 常驻进程启动后改前端 → index.html 引用 `?v=旧hash` 不变 → 浏览器永远命中旧缓存（服务端又无 Cache-Control）。
- 修复：删掉模块级常量，`static_asset_version()` 改为**每次服务 index.html 时动态算**（sha256 两个文件前 12 位，FileNotFoundError 兜底返回 "0"）；用法处 `data.replace(... static_asset_version().encode(...))`。改前端后 F5 即拉新，无需重启进程。
- 验证（8787 隔离实例）：改 styles.css 追加注释 → 版本号 `55a1e8fcf932→565958368b25`；还原 → 回到原值。与磁盘 hash 完全一致。
- 注意：**8765 是旧 Python 进程仍需重启一次**才会带上新 server.py；此前改的前端资源用户 Ctrl+Shift+R 硬刷即全部可见。
- 8765 已用系统 Python 3.13.15 (`C:\Users\admin\AppData\Local\Programs\Python\Python313\python.exe`) 重启完成（PID 30556 已 Stop-Process，PID 8765 重启后版本号 `v=55a1e8fcf932` 与磁盘 hash 一致）。之后改前端 F5 即生效。证据截图：`_iso_test/shots/live-8765-topbar.png`（CDP 在 9222 直接截 8765 真实实例）。

## 修正：多媒体产物不进"修改文件"总结（20:45，用户反馈"改过头了"）
- 用户诉求：生成图片/视频等多媒体用回**原来那套消息内产物卡片预览**（extract_attachments → metadata.attachments → mediaMarkup），不要被右侧文件面板/"修改文件"chips 双重接管。
- 修复（后端 `server.py`）：新增 `MEDIA_PRODUCT_EXTS`（.png/.jpg/.jpeg/.webp/.gif/.mp4/.webm/.mov/.m4v/.ogv/.wav/.mp3/.m4a/.ogg/.flac，与 extract_attachments 名单对齐）+ `_is_media_product_path()`；`file_changes_from_runs` 跳过媒体路径 → 生成物只走原附件预览。
- 修复（前端 `app.js`）：`fileChangesSummaryMarkup` 加 `FILE_CHIP_MEDIA_EXTS` 兜底过滤（历史消息 files 里混入媒体也不渲染 chip）。
- 单测：write img.png/shot_01.mp4/voice.wav + note.md + src/app.js → 只返回后两者。UI 断言：seed 会话注入 6 条 files（3 文本+3 媒体）→ chip 只剩 3 个文本，mediaChips=0。
- 8765 与 8787 均已用新 server.py 重启；验证脚本 `scripts/_probe_mediafilter.mjs`，截图 `_iso_test/shots/mediafilter-chips.png`。
- 多媒体过滤后全量重跑回归全绿：virtualize（clamp 1136 钳制 OK）、collapse（4 态 grid 断言过）、reopen（按钮三态过）、chatmax（865→1185px）、mediafilter（mediaChips=0）。无失效断言需改。

- 改「改」徽标从琥珀黄到淡青绿 mint：styles.css :root 加 --mint:#2fae9e / --mint-soft:#d9f2ec；line 101 desktop 改 var(--mint)，line 918 mobile 改 var(--mint)。截图 `_iso_test/shots/mediafilter-chips.png`。

- 顶栏「文件 N」按钮改回旧逻辑 + 视觉对齐 WorkBuddy「产物面板」：app.js updateFileTabsButton show 条件加 hasTabs（空会话不显示），reopenFilePanel 空 tabs 直接 return；styles.css .file-reopen-button 改为深色长方形（background var(--sidebar)、color sidebar-text、border transparent），has-tabs #fileTabsCount 改 var(--mint) 底白字加粗。验证：scripts/_probe_reopen2.mjs（连 8788，seed 在 _iso_test2），5 步全过。隔离目录从 _iso_test 迁到 _iso_test2：旧 _iso_test 的 8787 进程是沙箱孤儿（bash 26916→bash 30416→python 26748）杀不掉，改 _iso_test2 跑 8788 绕开。旧 _probe_reopen.mjs 断言已过期但未删除。截图 `_iso_test2/shots/reopen2-*.png`。

- 收尾：把硬编码 8787 / _iso_test 的 6 个脚本（regression-collapse、regression-virtualize、regression-files-panel、_probe_mediafilter、_probe_chatmax、_probe_grid）sed 改 8788 / _iso_test2；删除过期的 _probe_reopen.mjs（_probe_reopen2.mjs 头部加迁移说明）；给 _iso_test2 注入最小"UI 回归：修改文件与右侧面板"会话（1 user + 1 assistant 带 metadata.files=[rel.txt/note.md]）。重跑 3 个 regression + 4 个 probe 全过，mobile 端 openTabsHidden: true 验证新逻辑。_iso_test2 跑在后台任务 5VhbgF（重命名为 o2FSqm 后台任务）维持 8788。

- 全部撤回：`git stash push -m "2026-09-02 UI/媒体过滤/动态hash 未提交改动(回退保留)"` 把今天 5 个已跟踪文件（server.py/async_tasks.py/public 三件）的未提交改动存入 stash@{0}，工作区回到 HEAD 975ae80（Release 1.6.8-beta）。untracked（.workbuddy/_iso_test*/deepseek-harness/docs/scripts/skills 等）不动。恢复命令：`git stash pop`。8765 已用 1.6.8-beta 源码重启（新 pid 28648，系统 Python 3.13.15），服务版本号 v=0ce418e9b75c 与磁盘 hash 一致。注：8765 进程受沙箱 bash 链保护，Stop-Process/taskkill 均"拒绝访问"，需用户在任务管理器手动结束（本次用户已手动杀掉旧 pid 31536）。


