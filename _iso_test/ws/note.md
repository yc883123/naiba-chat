# 项目说明（markdown 富文本预览）

这里是**加粗**与 `行内代码` 的中文段落，用于验证面板把 .md 渲染成富文本而不是纯文本。

## 二级标题

- 列表项 A：面板应显示为项目符号
- 列表项 B：换行后保持缩进

> 引用块：右侧边栏「查看文件 + 手动编辑」能力验证。

```python
def hello(name: str) -> str:
    return f"你好，{name}"
```
