# Naiba Chat 2.3.7 Beta

Naiba Chat 是运行在 Windows 本机的通用 AI 自动化工作台。它把在线或本地模型、内置工具、后台任务、Skill、MCP、视觉工具和文件产物统一到一个对话界面中。

2.3.7 Beta 在 2.3.6 的基础上新增**外观主题与皮肤**：设置页置顶的「外观」区块可选**主题模式**（跟随系统 / 日间 / 夜间——跟随系统会随 Windows 的浅色 / 深色设置实时切换）与**四套界面皮肤**（紫藤 / 海洋 / 玫瑰 / 森林，只换品牌色相：按钮、选中态、消息气泡等，版式与信息层级不变）；偏好保存到服务端（`config.json` 的 `appearance`），同一实例的电脑与手机端保持一致，浏览器本地另存一份用于首屏无闪烁，面板内可「保存」「恢复默认」，非法或历史配置值自动归一化。设置导航同时改为分组（常规 / 模型与 Agent / 工具与联网 / 数据与隐私 / 关于）并新增关键词搜索。2.3.6 起已具备：**「编辑」= 完整输入区就地编辑**——点「编辑」后底部整块输入区（输入框、`/` Skill 引用、`@` 工作区引用、图片与文件上传、待发送附件、思考强度与 meta 行）搬进那条用户气泡，编辑时照常粘贴 / 拖拽上传、增删附件、用 `/` 与 `@` 引用，Enter 重新发送、Shift+Enter 换行、Esc 取消，退出后恢复进入编辑前的底部草稿与附件；`/`、`@` 弹层稳定盖在「正在编辑此消息 / 取消」之上。2.3.5 起已具备：**AI 回复「重新生成」**、**用户消息「编辑」回归**——两者的重发都从**那条提问**处截断（而不是从回复处），之前的问答逐字节保持原样、前缀缓存继续命中，反复点也不会累积重复提问；引用与附件一并保真（`/ref` 技能引用与 `@` 工作区引用逐字保留，图片 / 文件原样随行，纯附件轮次也能重发）。HTTP API 与事件协议版式不变。以下是 2.3.4 起已具备的能力：**权限判定与实际执行统一以当前会话工作区为准**——此前切换过工作区的会话会把工作区内的文件判成越界、反复弹确认（现在 `read_file` / `list_directory` / `search_files` / `write_file` / `edit_file` / `read_pdf` 判定与执行共用同一个根），**点「允许执行」后也不再丢失运行上下文**（工作区、取消信号、任务归属随确认一起暂存），子 Agent 同步改用会话工作区；越界确认卡片直接写明「当前工作区」，另新增可选权限诊断开关（`NAIBA_DEBUG_PERMISSION=1`）。手机端顶栏新增**收起开关**，把被顶栏吃掉的高度还给会话区；审批档位提示文案写明实际边界（自动＝工作区内放行、越界仍确认；完全＝不再询问、可访问工作区外）。HTTP API 与事件协议版式不变。以下是 2.2.3 起已具备的能力：**Skill 删除**重构——删除改为确认对话框，可选「移至回收目录（可恢复）」或「彻底删除」，设置页新增回收目录列表（可一键恢复或清空），并修掉「同一 Skill 有多份副本时点了删除立刻复活」与「删掉的内置 Skill 下次启动又回来」两个顽疾；**会话模型选择**收紧为「只从当前 API 检测到的模型里选」，未检查模型或选中项不在目录里会拦下发送（不再把未经验证的模型名发给服务端），并修掉供应商更换默认模型后旧会话仍继续发送旧模型名的问题；冻结版打包补齐 multipart 解析器（修复安装版上传附件失败）。手机端「功能对等」——手机经局域网打开后，电脑能做的事手机都能做，只换形态——右侧文件面板从「整体隐藏」改为全屏抽屉、消息末尾「本轮修改的文件」条目恢复可点、桌面右侧的对话刻度轨换成顶栏「轮次」下拉、触摸目标统一不小于 44px；同时修复 **Skill 安装目录归属**（散装上传先建独立子目录、删除带归属校验，杜绝「删一个 Skill，全部消失」，升级自动迁移历史裸定义文件并保留引用），增强**在线模型稳健性**（Codex Responses 聚合事件回填、空流按瞬时故障重试、HTTP 5xx 退避重试并落盘失败请求体取证），并做界面打磨（审批模式改为上拉框、输入区模型只从当前 API 的模型目录里选、API 供应商卡片更紧凑）。详见下方「2.3.7 Beta 主要能力」。

## 2.3.7 Beta 主要能力

- **外观主题与皮肤 + 设置导航搜索（本次重点）**：设置页置顶新增「外观」区块——**主题模式**可选「跟随系统 / 日间 / 夜间」（跟随系统随 Windows 深浅色设置实时切换，无需重启），**界面皮肤**可选「紫藤（默认）/ 海洋 / 玫瑰 / 森林」（只换品牌色相——按钮、选中态、消息气泡、链接等，版式与信息层级不变）；偏好保存到服务端（`config.json` 的 `appearance`），同一实例的电脑与手机端保持一致，浏览器本地另存一份用于首屏无闪烁；面板内可「保存」「恢复默认」，未保存时提示「有未保存的外观更改」，非法或历史配置值自动归一化（`DARK` → `dark`、未知皮肤 → 紫藤）。设置左侧导航同时改为按「常规 / 模型与 Agent / 工具与联网 / 数据与隐私 / 关于」分组，并新增关键词搜索（输入「深色」「代理」「备份」等词只留相关分类，无匹配时提示「没有匹配的设置」）。
- **「编辑」= 完整输入区就地编辑（2.3.6 重点）**：点击「编辑」后，完整的 `.composer-wrap`（输入框、`@`/`/` 引用、图片与文件上传、附件列表、思考设置和 meta 行）从底部移动到该用户气泡内，底部仅留隐藏占位；气泡展开为全宽编辑态，编辑头提供取消按钮。编辑时照常粘贴 / 拖拽上传、增删附件、用 `/` 与 `@` 引用，**Enter 重新发送、Shift+Enter 换行、Esc 取消**；原消息附件随列表带出，可逐项删除，也可继续添加；取消时恢复原草稿与附件，确认时按当前列表重发。**并修掉编辑态里 `/`、`@` 弹层与「正在编辑此消息 / 取消」互压的层级问题**——弹层稳定盖在编辑头部之上，弹层关掉后「取消」恢复可点。底部输入区不再与会话内编辑框同时显示。
- **AI 回复「重新生成」（2.3.5 重点）**：每条完整 AI 回复末尾、「复制」右侧新增「**重新生成**」（操作区顺序：复制 · 重新生成 · 新会话）——点一下就用这条回复对应的**那条提问**原样重新作答，不必再手动复制粘贴重问。**截断点是提问本身、而不是回复**：重发时先删掉那一问及其之后的全部消息，之前的问答**逐字节保持原样**，因此**前缀缓存继续命中**；也正因为截断点落在提问上，反复点「重新生成」不会像「从回复处截断」那样越点越多重复提问。只有**非最后一轮**才弹确认框（并写明「之后还有 N 条会一并删除」），**最后一轮一键直发**、不打断；截断不可逆这点已在确认框里讲明。会话运行中该按钮自动隐藏（与「分支」「新会话」同口径）。
- **用户消息「编辑」回归（2.3.5 重点）**：每条用户消息下方恢复「**编辑**」（操作区顺序：编辑 · 分支）——在该条消息的位置**就地展开编辑框**（不跳到底部输入框），改字后按同一套重发链路重开。原文回填取用户原样文本（含 `/ref` 技能引用与 `@` 工作区引用），附件也在编辑框里带出。
- **编辑 / 重发的引用与附件保真**：`/ref` 技能引用与 `@` 工作区引用**逐字保留**——`@` 引用仍由后端在发送时按该会话工作区替换为绝对路径，重发结果与首次发送一致；编辑框带出该轮原有的图片 / 文件（与「分支」同口径，含缩略图字段），重发原样随行；**纯附件轮次**（只发文件、不写字）也能重发，不再被「内容不能为空」误拒。


- **工作区判定与执行同源**：权限判定与实际读写统一以**当前会话的工作区**（Run 快照 `workspace_dir`）为准——此前判定用当前工作区、实际读写却可能按启动时的工作区解析，切换过工作区的会话会出现「工作区内的文件被判越界、反复弹确认」；现在 `read_file` / `list_directory` / `search_files` / `write_file` / `edit_file` / `read_pdf` 的判定与执行**共用同一个根**。越界确认卡片直接写明「当前工作区：…」，一眼看出是拿哪个目录在比；另新增可选诊断开关（`NAIBA_DEBUG_PERMISSION=1`）输出执行器身份、请求路径（原始与解析后）、当前工作区、允许根与命中结果，用于定位「为什么被判越界」。**语义与安全边界不变**：自动＝工作区内免确认、工作区外（读也算）仍必须确认；工作区内指向外部的链接（junction / 符号链接）一律按真实目标判定、仍算越界（新增路径矩阵护栏测试）。
- **修复：点「允许执行」后运行上下文丢失**：待确认的工具调用现在连同完整运行上下文（工作区、取消信号、任务归属等）一起暂存，批准后按原上下文执行——此前批准后上下文为空，依赖它的能力（产物目录、后台任务归属、上下文重置等）会行为异常；同步确认与「已确认、后台执行」两条路径都已修正。
- **修复：子 Agent 改用会话工作区**：子 Agent 此前按启动期默认工作区判定与执行，会话工作区内的路径会被判越界，解压/安装 Skill 还会直接报「无法确定工作区目录」；现在子 Agent 继承该会话的工作区与审批档位（权限不超出父级），工作目录与产物位置与父会话一致。
- **手机端顶栏可折叠 + 审批文案明确**：手机（≤760px）顶栏新增收起开关——收起后隐藏整条操作区（轮次 / 文件 / 任务 / Skill / MCP / 刷新临时让位，点细条即回），模型与 Agent 两个下拉并回第一行，把被顶栏吃掉的高度还给会话区；选择记在本地、刷新后保留，桌面端不渲染该按钮。审批档位提示文案写明实际边界：自动＝「工作区内自动放行，越界仍确认」、完全＝「不再询问，可访问工作区外」。


- **会话模型下拉可随时重新检测**：会话底部「模型」下拉末尾固定一项「**↻ 重新检测模型**」——选中它即**强制重新拉取**当前 API 的模型目录（绕过「同一 API 本页只拉一次」的缓存限制），拉取完成后**自动还原你刷新前的选择**。供应商新上架的模型因此立刻可选，不必再进设置页 → 编辑供应商 → 点「检查模型」。该入口失败会明确出声：拿不到目录提示具体原因、尚未配置 API 提示去设置页、目录为空提示「该 API 当前没有可用模型」；它只是一次动作、不是模型，会从选择出口被抹掉，**不会被写进会话、也不会发送给服务端**。
- **修复：空模型目录不再被当作「已查过」**：此前判断能否复用目录缓存的条件是「本页有没有查过这个 API」，而不是「缓存里有没有模型」——某些 API 在还没有可用模型时（如本地模型尚未加载）返回空列表，这一页就再也不会重拉，下拉框永久停在「请先在设置中检查模型」，即使模型随后已经可用。现在改为「缓存里确实有模型才复用」，空结果会自动重新拉取。
- **行为变更：未选择模型时不再默认选中目录第一项**：会话没有明确选择过模型时，下拉框不再自动选中检测目录的第一项（该顺序由供应商接口的返回顺序决定、没有任何含义——此前 teynex 这类供应商的新会话会静默用上比你配置更弱的模型），改为显示禁用占位项「请选择模型…」；已保存的模型仍优先展示，不在检测目录里时依旧置顶「已保存：X（请重新检查模型）」。**影响范围**：所有尚未选择过模型的会话（含历史会话）都需先在下拉框里显式选一次才能发送。
- **Skill 删除重构**：删除 Skill 改为确认对话框——明确「移至回收目录（可恢复）」与「彻底删除（不可恢复）」两种方式，并列出该 Skill 被哪些 Agent 引用（删除会一并移除引用）；设置页「已安装 Skill」下方新增**回收目录**列表，显示已回收的 Skill（名称 + 原路径），可一键**恢复**（移回托管目录并重新启用）或**清空回收目录**（永久删除）。**修掉「点了删除却没删掉」**：同一 Skill 在多处目录（或打包内置 + 托管副本）各有一份时，只搬走其中一份会让它立刻从另一份复活——现在这类 Skill 统一按 ID 隐藏（可在「已隐藏 Skill」里恢复），一次点击即从列表消失且重启不回来，内置 Skill 只处理托管副本、程序包原件不动；**启动时的内置同步与旧数据目录合并改为跳过「已删除（隐藏）」的 Skill 目录**，删掉的内置 Skill 不再被重新写回。
- **会话模型选择收紧（行为变更）**：会话底部「模型」下拉**只列当前 API 上检测到的模型**——移除供应商默认模型兜底与手动输入；**未在设置中「检查模型」或选中项不在检测目录里时，发送会被拦下**并提示「请先在设置中检查模型，并在会话底部选择检测到的模型」，不再把未经验证的模型名发给服务端；历史会话保存的模型若不在检测目录里，置顶显示「已保存：X（请重新检查模型）」，点「检查模型」拿到目录后自动刷新会话下拉；切换 API 后清空该会话的模型选择，等待新 API 的检测目录填充。
- **供应商默认模型换代不再影响已有会话**：此前在输入区没主动选过模型的会话，会把供应商当时的默认模型名一并落库成会话级覆盖；供应商之后改了默认模型，这些会话仍会继续发送旧模型名。现在保存 API 配置时若默认模型被改写，只清空「覆盖值恰好等于旧默认模型名」的会话（它们本就在隐式跟随），显式固定成其它模型的会话保持不变；修正为静默处理（不推进会话更新时间、不重排侧栏），并把图像能力重置为待重探。
- **冻结版打包补齐 multipart 解析器**：PyInstaller 显式收集 `python_multipart`——此前它是 `naiba.http` 的懒加载导入、未被自动打进包，可能导致安装版上传附件失败。
- **单文件 Skill 导入宽松化**：单个 `.md` 与 zip 里顶层散放的 `SKILL.md` 不再强制 YAML frontmatter——自动取首个标题作为 `name`、首个非标题段落作为 `description` 补出最小 frontmatter，正文原样保留，裸 Markdown 也能直接安装并在列表里正确显示。
- **异步任务（Job）韧性修复**：ComfyUI 批量生成每轮同时核对 `/history` 与 `/queue`——**既不在历史也不在队列**的 prompt 判定为已被 ComfyUI 丢弃（服务重启或队列被清空），按预算（`max_resubmit`，默认 1，持久化在 checkpoint）**自动重新提交同一工作流**继续生成，预算用尽才记为「ComfyUI 已丢失该任务」并正常收尾，不再空转到 `wait_timeout`（默认 2 小时）——修复「任务面板长期挂着『运行中 · 完成 5/8』且永远不动」；已失败的镜头也计入完成集合，恢复后不会重复轮询、把同一处错误反复累计；队列查询失败时保守处理、不误判。**启动时的中断恢复改为幂等**（`resumed_into` / `resume_skipped` 持久标记），且 checkpoint 完全相同的中断记录视为同一批的重复副本、只恢复最新的一条，杜绝「重启一次任务翻一倍」（实测此前一批 8 段生成被放大成每对话 13 份）。**后台子 Job 不再占用对话的活跃 Run 互斥位**——只有顶层 Run 占用对话，长跑或卡住的异步任务不会再让该会话发不出新消息（此前会 409 拒绝且提示语误导）。任务面板恢复**「停止」按钮**（新增 `POST /api/jobs/{id}/cancel`，走 Job Registry 会真正中止 worker；`chat` / `plan_execute` 仍走原取消接口）。新增 8 例守门测试覆盖上述四条不变量。
- **手机端「功能对等」（不删能力、只换形态）**：手机（局域网访问）不再是「缩小版电脑」——电脑能做的手机都要能做，放不下的控件换等价形态。右侧文件面板从「整体隐藏」改为**全屏抽屉**（与桌面共用同一个面板与开合链路，只换呈现形态），消息末尾「本轮修改的文件」条目在手机上恢复可点（此前被 `pointer-events` 关掉），点开即打开文件抽屉；桌面右侧的「对话刻度轨」换成顶栏**「轮次」下拉**（列出「第 N 轮 · 用户消息摘要」，选中即跳到该轮，滚动时自动显示当前所在轮次，不足 2 轮隐藏；复用刻度轨同一份轮次模型与跳转函数，跳转到未渲染轮次会先扩懒加载窗口再定位）；触摸目标统一 ≥44px（顶栏按钮、侧栏抽屉开关、输入区图标与发送、文件面板关闭）；跨越 760px 边界不再关掉文件面板，「手机端」判定只有唯一来源（CSS 760px 与前端常量同界），窄屏也不隐藏任何文字标签。
- **Skill 安装目录归属修复（防「删一个 Skill，全部消失」的数据事故）**：单文件 `.md` 导入与顶层散放 `SKILL.md` 的 zip 以前会把定义直接写在托管目录根下，导致该 Skill 的 `root` 等于整个托管目录，删除时把**所有 Skill 一起搬走**。现在凡有文件直接落在导入根的上传都先建独立子目录（目录名取 frontmatter `name`），带顶层目录的文件夹上传保持原样、不多包一层；删除前校验归属（移动目标必须严格位于托管目录之下，`root` 等于托管目录时退化为只回收该定义文件），越界一律拒绝；启动时自动把历史裸 `SKILL.md` 迁入同名子目录，并同步 Agent 固定 Skill 引用与隐藏 ID。
- **在线模型稳健性（聚合回填 + 空流重试 + 5xx 取证）**：Codex Responses 流补齐**聚合事件回填**——仅收到 `response.output_item.done` / `response.completed`（没有逐字增量）时也能正确提取正文、思考与 reasoning item id，并把单独出现的 `function_call` 组装为工具调用（同一 message 只回填一次，`response.incomplete` 的被截断内容不作为成功正文）；该格式下「流式消费完但既无正文也无有效工具调用」按瞬时故障退避重试，其它在线格式行为不变；HTTP 5xx（含 500）与 429/502/503/504 同等退避重试（1.5s / 3s，最多 3 次尝试），思考回传类 400/422 与全部 5xx 都把失败请求体落盘取证（超长字段截断、不含 API Key）。
- **界面打磨（审批上拉框 / 模型只从目录选 / 供应商卡片更紧凑）**：审批模式由平铺四段改为**上拉框**——收起态只显示当前档与风险配色，点开才在按钮上方弹出列表（浮层置顶、不被输入区裁剪，贴顶自动下翻、夹在视口内），切换会话、打开其它浮层、Esc、点外部都会收起；输入区「模型」选择器移除手动输入，模型一律从**当前 API 的模型目录**里选，历史会话保存的模型若不在目录里会置顶「已保存：X」并默认选中（避免被静默换掉），并修掉「目录返回时会话已切换」导致旧会话模型被顶到新会话的问题；API 供应商卡片压成两行（名称 + 格式标签与当前角标），模型名不再上卡片，卡片更矮（116px → 88px）。

- **会话级模型选择（API 与模型分离）**：顶栏「模型」下拉改为 **API 下拉**——只负责选连接配置，选项直接显示 API 名称（不再拼「名称 · 模型」），切换后提示「API 已切换」；输入区新增 **「模型」选择器**：打开会话时自动拉取当前 API 的模型目录（`/api/providers/models`），**只列出检测到的模型**（不再回退 API 配置里的默认模型，也没有手动输入入口），下拉末尾固定一项「↻ 重新检测模型」可随时强制刷新目录，未明确选择过模型时显示占位项「请选择模型…」；下拉选择即保存到该会话；模型选择**按会话记忆**（`conversations.model_name`，分支对话继承同一 API 与模型名，互不干扰），顶栏切换 API 时该会话的模型名一并清空重选；**切换 API 或模型名后自动重新探测图像能力**（`chat_supports_images` 重置为待探测，发送时按新模型真实能力判断——换到文本模型不再误发图片、换到多模态模型不再被当作纯文本）。数据库 v17 幂等迁移，升级自动完成。

- **长会话模式（翻历史 + 重置上下文）**：Agent 工具集新增「长会话」一组——`find_conversations`（列出最近会话的 id/标题/时间/条数与末条预览，可按标题匹配）、`recall_history`（按关键词检索：不给会话 id 就全库搜、每个会话最多 3 条片段；给了 id 就只搜那个会话、按时间正序、片段更长）、`read_conversation`（按序号区间读历史会话原文，用于复核片段），加上 `reset_context`。配套新增 **「长会话模式」预设** = 标准模式 + 这四件套。检索结果会明示"检索了全部 N 个会话 / M 条消息"、截断显式、会话 id 不存在直接报错；**当前会话的命中会标注「在上下文中」或「已划出上下文」**——被「新会话」分割线划出去的老消息不会被当成你还记得的内容。
- **长会话懒加载**：打开会话只渲染最近 10 轮，向上滚动时自动往前预渲染（每次 10 轮，视口不跳动）；渲染窗口**以「轮」为边界**，所以「新会话」分割线永远和它下面那条回复一起出现、不会被拆到两段里。右侧刻度轨仍然覆盖全部轮次，点没渲染出来的轮次会先渲染再跳过去。
- **交接文档快捷消息**：快捷消息面板新增内置预设「写交接报告并 reset_context」（在「快捷消息」列表里，带「默认」标签）——点一下即插入到输入框，要求模型把交接报告写进工作区（目标/进度/关键文件路径/已定约束/后台任务/下一步）并用 `read_file` 复核后调用 `reset_context`；**这条预设和用户自建条目同权**：可编辑、可删除，改动与删除都记在你的配置里，软件更新不会覆盖也不会复活。
- **同一窗口内「新会话」分割线**：每条完整 AI 回复末尾、「复制」右侧新增「新会话」按钮——在**任意位置**划一条分割线，**此线以上的消息不再进入模型上下文，线以下的消息仍保留在上下文中**（所以可以只裁掉前面跑题的部分、保留最近的进展），聊天记录一条不删、分割线可逐条撤销；允许多条分割线并存，靠时间与位置区分，最新那条决定当前上下文起点。**模型也能自己重置**：Agent 工具集里勾上 `reset_context` 后，模型可先把交接文档写进工作区、再调用它（**文档不存在或为空会被拒绝**，每轮最多成功一次），本轮立即收尾并在该条回复下画出「模型重置」分割线；随后**种子消息自动填入输入框**（默认含交接文档路径 + 「请先读取再继续」 + 仍在运行的后台任务清单，模板可在「设置 → 运行设置 → 新会话种子消息」里改，占位符 `{handoff_path}` / `{task_count}` / `{task_list}`），由你确认后点发送开始新一段。
- **上下文用量提醒**：输入框旁的上下文圆环随**每次模型请求**刷新（不再等整轮结束），**输入框下方「思考 自动」右侧还常驻一行实时百分比**（如「上下文 8.2%」，达 70% 转黄、90% 转红，悬停看完整 `已用 / 上限`），达到「设置 → 运行设置 → 上下文提醒阈值」（默认 80%，0 = 关闭提醒）时弹窗提醒——**正在运行的会话**涨过阈值即弹，**已结束的会话**改为点发送时才弹（可「继续发送」，被拦下时草稿不清空）；距上次提醒再涨 5 个百分点才再次提醒，换会话后重新计算；弹窗建议让模型撰写交接文档并开始新会话。
- **在线模型 5xx 自动重试**：供应商返回 5xx（如 HTTP 500）时不再直接终止整轮，改为按瞬时故障退避重试（1.5s / 3s，最多 3 次尝试）并在状态栏提示重试进度；失败请求体落盘到进程工作目录的 `naiba-model-error-payload.json`（超长字段截断、不含 API Key），便于复现定位。
- **设置页卡片化**：API 供应商与 Agent 管理改为卡片网格（一行最多三张、末尾固定「添加 API」/「新增 Agent」卡片、卡片右上角 × 删除），点卡片才弹出该条目的设置弹层并加载已有配置；字段与文案保持原样，只换展示方式。**默认 Agent 不再在卡片上标「默认」角标或加高亮**——默认项由顶栏 Agent 选择器体现，卡片上任何强调色都会被误读成「当前选中」；**每张 Agent 卡片在「固定 Skill」下方显示它当前用的工具集名称**（命中内置预设就显示预设名，如「工具集：标准模式」；空配置显示「未限制（全部工具）」，其余显示「自定义 · N 个工具」）。
- **Agent 自动 ID 与自定义头像**：新建 Agent 不再手填 ID（保存时后台分配持久化唯一 ID，已有 ID 显示在弹层副标题）；Agent 编辑弹层新增「自定义头像」，上传图片自动中心裁切为 256px WebP，使用该 Agent 的会话会把助手气泡的「AI」圆标换成这张头像，Agent 卡片上也显示缩略图。
- **内置 Agent 预设下线**：四个内置预设（dsh-standard / dsh-code / dsh-minimal / dsh-cordis）不再自动出现；此前编辑过、已存进配置的副本会摘掉「内置」标记、变成普通可删除的 Agent（内容保留），引用它们的会话自动回退默认 Agent。
- **工具与 Skill 选择器改版**：固定 Skill 改为卡片网格并与工具集等高；工具集分组标题/说明/计数一行呈现，「视觉」分组补说明并前移到「命令执行」之后；展开后的工具卡片更紧凑，只有展开的那一组标题条变浅灰以区分层次；修复列表在弹层里被压成一条线、点不动的问题。**工具集分区改为「卡片态 ↔ 编辑态」两态同框切换**：弹层高度恒定不跳，默认只显示 4 张只读预设卡（只读 / 标准 / ComfyUI 联动 / 全能）与「我的工具集」卡片、末尾一张虚线「添加自定义工具集」卡；点任意一张卡都会先按下、卡片整块向上滑出、工具列表从下方滑入，命名栏自动填上被点卡片的名字，勾选就是该卡的工具（可继续增删；「添加自定义工具集」固定以标准模式为起点）；选完可存成「我的工具集」复用，内置预设只读不可改、自定义工具集可删除。**「我的工具集」存后端 config.json（`tool_sets`），不再存 localStorage**——冻结版 pywebview 默认 `private_mode=True`，WebView2 的 localStorage 每次退出都会被清空，此前保存的工具集重启后会消失；旧版本留在 localStorage 里的数据启动时自动搬到后端一次。
- **设置页右键菜单修复**：设置页文本框右键菜单恢复可用（撤销/重做/剪切/复制/粘贴/删除/全选）——此前被模态弹层整体盖住；同时不再误弹会话页的选区菜单；数字类输入框右键「复制」改为复制整值。
- **桌面壳持久化**：内嵌窗口的 WebView2 改用持久 profile（此前 pywebview 默认私有模式，profile 放在临时目录、退出即删），侧栏宽度、文件面板宽度、顶栏 Skill 勾选、交互模式这些本机偏好不再每次启动重置。
- **会话页对话刻度轨**：会话右侧新增一列横条，每个用户轮次一条，滚动时视口中心所在轮次加粗凸起；长会话只显示附近 30 条，悬停显示该轮概要（第 N 轮 + 用户消息一行、AI 回复两行），点击跳到该轮；消息列整体左移约 13px 让位，窄屏自动隐藏。
- **后端全面模块化重构（阶段 0–4）**：原先约 2.4 万行堆叠在根目录的后端代码迁入 `naiba/` 包（组装根/传输层/配置/存储/运行/工具/技能/模型/视觉/网络等领域分层），依赖图单向无环、机械可验证（DAG 守门测试）；`server.py` 收口为 136 行门面，HTTP 层仅剩薄包装与传输职责，业务逻辑全部下沉至 `NaibaChatApp` 与领域模块。
- **路径与配置**：`PathContext` 统一管理源码/冻结双模式目录（冻结版固定 `%LOCALAPPDATA%\NaibaChat`，旧数据迁移与自定义数据目录切换语义保留）；`ConfigStore` 迁入 `naiba/config.py`；静态资源版本哈希改为 lazy-once。
- **契约化**：`RunContext`（18 键 + 校验工厂）/`EventType`（44 种，与前端反查对齐）/`MetadataKeys` 显式化；`AppContext`/`ConfigView` Protocol 注入收窄（14 处 `app: Any` → `AppContext`）。
- **修复与清理**：模型测试连接 `NameError`（`@staticmethod`+`self` 错配）与上传 500（`self.app` 残留引用）等搬移期缺陷；诊断开关兜底统一；死代码清理（MCP 诊断管道、废弃插话等）。
- **守门测试**：新增权限矩阵（full/confirm/auto/deny 全模式）、装饰器-绑定一致性、`self.app` 残留引用扫描等网格化守门，共 83 个单测；全量功能回归（11 域 48 项）通过。
- **工具系统增强**：视觉类工具单入口（vision_analyze 按会话模型能力自动分流 + vision_image_ops 本地图像计算）；移除 call_mcp 网关；多模态/文本模型同一工具集，退役工具自动迁移/提示；DeepSeek 思考模式工具轮兼容修复（reasoning item 按官方 Schema 回传：`reasoning_text` 内容块列表 + 服务端唯一 id，旧历史无 id 自动合成兜底）；Harness 兼容别名（read/write/edit/grep）从模型可见工具清单隐藏（执行兼容保留）；工具返回对模型可见性统一（机器字段剥离、截断带标记、前端展示与模型上下文同源；vision_image_ops 产物路径对模型保留，供其引用裁剪/热力图产物）；glob_files 并入 list_directory（单入口枚举）。
- **行为兼容**：对外 HTTP API 路径、事件流协议与前端完全不改；13 个根模块保留兼容 shim（`from naiba.X import *`）；`launcher.py`/PyInstaller spec 零改动。
- **历史数据管理**：推理流保持逐词实时显示、回复完成后自动整理为整段思考（事件流不再逐 token 累积）；终态任务快照自动收缩；升级时自动压缩存量历史数据（迁移前先整库备份到 `data/backups`）；设置页「历史数据管理」可查看数据库占用并一键压缩回收空间；实测 400MB 存量库压缩至 ~90MB，继续会话发送给模型的历史消息逐字节不变（前缀缓存命中不受影响）。
- **时间线与用量可观测**：活动时间线规则定稿（思考块一律折叠、思考/工具严格物理序、最终答复固定时间线末尾、每次模型请求开始处以横线分割，与用量明细的请求序号对应）；流式期间消息尾端常驻用量框，每完成一次模型请求即刷新缓存命中率/tokens（最后一次请求口径）与**生成速率**（输出 tokens ÷ 该次请求耗时），完成后显示本轮总耗时、**平均速率**与完成日期，「请求明细」展开逐次请求的 token/命中率/速率/耗时；会话顶部新增「首次请求上下文」折叠卡，可查看第一轮发送给模型的内容（完整系统提示词原文、工具 JSON 定义、生成参数），第一轮完成后即时显示（终态事件后立即拉取，切换会话时同样刷新）。
- **文件上传流式化**：上传改为 multipart 流式直传（内存更省、真实传输进度、单文件可取消、多文件并行上限 3、80MB 前端前置校验）；上传内容自动去重（重复文件复用既有缓存）、按日分目录存储、移除待发送附件时自动清理未引用文件；上传前/上传中的图片缩略图不再请求空路径。**待发送附件在输入框上方以竖直列表显示**：一行一个文件、固定高度（约 4 行）后内部滚动，文件名过长自动截断（悬停看全名），图片附件显示预览缩略图（点击看大图）、其它文件显示文件图标，每行右侧可单独移除；附件行用浅底卡片 + 描边与投影，和输入框层次分明。
- **缓存管理**：图片/PDF/视频/音频等统一「缓存文件」统计（设置页打开即刷新实时值）；超限自动清理（阈值默认 256MB、设置页可调/填 0 关闭）**只删未被消息引用的最旧文件**——历史消息引用的附件永不自动删除；手动清理与自动清理共用同一阈值口径，界面明确提示「按时间从旧到新、不区分是否被引用」。
- **PDF 解析与识别**：内置 PDF 工具三件套——`read_pdf`（文本层提取：页标记、单次 30000 字符/50 页、截断续读提示、扫描版自动引导转页图）、`pdf_render_pages`（页面渲染为 PNG 页图，可复核的 AI 看图素材）、`pdf_zoom_region`（局部高清放大：小字/表格/图表看不清时矢量源重渲精读）；上传或提及 PDF 后模型按需提取文本/渲染页图并用 `vision_analyze` 识图；页图缓存在受管数据目录（自动清理覆盖），不污染工作区；`read_file` 对 PDF/图片/二进制文件不再输出乱码，改为明确提示并引导对应工具。
- **多图分批读取**：`vision_analyze` 单批默认 4 张（装载/分析两路径统一），超出自动分批注入并带批次标注（共 N 张/已展示前 M 张）——多页 PDF、多图分析时模型清晰知道"还有剩余",不再因静默截断而误解；用户上传附件与宿主托管产物（uploads/generated）在读取策略中视为可信根，免越界确认（真正的工作区外路径仍确认）。
- **纯附件发送**：输入框不写文字也能直接发送文件/图片（拖入、粘贴、附件按钮均可）；发送按钮按可用性变化——无可发送内容时灰暗禁用，可发送时恢复实心主色，上传未完成或上下文已满时禁用并给出原因提示；仅发附件时模型侧会明确收到「用户未输入文字，只发送了以下附件」+ 附件路径，不会凭空脑补用户诉求；纯附件首轮会话标题自动取首个附件名；发送按钮状态同样跟随程序化填字刷新（快捷消息插入、技能与 `@` 引用回填、选择按钮写入后即时更新）。
- **@ 引用工作区文件/目录**：输入空格 + `@` 弹出工作区目录列表（懒加载，只列当前目录，隐藏 `.` 开头的条目），方向键选择、Tab 进入子目录 / Shift+Tab 返回上级、Enter 或点击即引用；目录条目右侧另有箭头按钮可直接进入（手机端无 Tab 键的替代入口），列表首项为「返回上一级」；选中目录时名称左侧显示 Tab 键提示。引用后输入框与消息气泡保留 `@相对路径` 高亮，发送给模型时自动替换为该文件的绝对路径（目录保留尾部分隔符，便于模型处理整目录）；只有工作区内真实存在的路径才会被替换，邮箱等普通 `@` 文本不受影响。
- **快捷消息面板**：输入区底部新增「快捷消息」按钮，点击展开常用提示词列表（**独立列表**，与「开始新对话」页的自定义指令互不影响，各自增删改）；**每条只有正文、不用起标题**（列表以正文首行作标题、其余行折叠预览，新增/编辑弹窗只填正文）；列表默认按「使用次数 × 2 + 新鲜度加分」排序——常用条目稳定靠前，新添加的条目在 7/30/90 天内有曝光窗口不会永远沉底，并列时新增的在前；点击条目把正文插入输入框（可继续编辑再发送）并累计使用次数，行内 `✎` 编辑、`✕` 删除；面板在窄屏上自动贴边对齐，触屏下操作按钮常显。
- **图片查看**：点击会话里的图片打开大图，灯箱内可用左右按钮、`←`/`→` 键或**点击屏幕左右半屏**连续浏览当前会话的全部图片（按在对话中出现的顺序，首尾循环），底部显示 `当前 / 总数`；**滚轮缩放、缩放后拖动平移、双击复位**（触屏支持单指拖动、双指捏合、双击切换），`Esc` 或点击空白处关闭。输入区待发送的附件与右侧文件面板里的图片仍按单张查看。
- **工具产物就地显示（多媒体内嵌）**：工具产出的图片/视频/音频不再等整轮结束才集中堆到消息末尾，而是**紧跟产生它的那次工具调用就地显示**——流式期间工具一返回就出现，工具块保持折叠、媒体始终可见，同一张图不会重复显示（消息末尾网格自动跳过已就地渲染的附件）；提取策略由工具在注册表显式声明（`metadata["media"]`：`inline`/`intent_gated`/`never` × `none`/`scan`/`structured`），不再按工具名枚举猜测，且只对成功的工具结果提取（失败信息里的路径不会变成破图）；单次调用按类型分桶上限（图 20 / 视频 8 / 音频 8）**先截断再入缓存**（列一次目录不会把几百张图拷进缓存卡住），超出时明确提示「共 N 个媒体，仅显示前 M 个」；`/api/file` 支持 Range 请求（视频/音频进度条可拖动、大文件不整包进内存），用户上传与工具产出的视频/音频都能在气泡里直接播放；GIF 保留动画并生成首帧缩略图（此前缩略图必然 404 破图）；后台任务（ComfyUI 等）产物在任务完成后**自动挂回发起它的那条消息**、挂到对应工具调用下方，无需再追问模型；缓存改为按内容哈希（修复 ComfyUI 同名覆盖时命中旧图）并修复带查询串的图片 URL 被截断（表现为「任务完成却看不到图」）。
- **开始页预设可编辑**：开始页内置预设（通过 MCP/HTTP 调用 ComfyUI、设置本地 Comfy MCP、列出可用工具/所有文件、等待用户指令）与自建指令同权——悬停即可编辑与删除；删除后底部出现「恢复默认预设」一键找回；升级后新增的内置预设自动出现，已删除的按预设 id 记录、不会复活；编辑内置预设保留其说明与图标。
- **思考强度交互**：思考按钮点击只做一件事——展开/收起强度列表（此前收起时会顺带切档）；列表高亮当前档位，输入框下方与「就绪」同行常显当前强度（`思考 自动/关闭/低/中/高`）。
- **Skill 脚本编码规范**：安装/编辑 Skill 的说明补充脚本规范——脚本统一放 Skill 目录的 `scripts/` 子目录，读写文件显式指定 UTF-8（冻结版默认编码跟随系统 locale，裸 `open()` 写中文会乱码）；同时修复 Skill 脚本输出中文乱码（隐藏入口强制子进程 UTF-8 输出）。
- **顶栏精简**：「任务」「Skill」计数改为普通数字文本（不再用胶囊徽标，按钮也不再重复「Skill」前缀）；MCP 状态**只看圆点颜色**（绿=已连接、红=未连接或出错、黄=连接中、灰=未配置服务，鼠标悬停看具体状态）；「刷新」回到顶栏常驻并移除「⋯」溢出菜单，顶栏「卸载模型」按钮删除（本地模型卸载仍在「设置 → API 供应商」）；文件面板按钮改用与其它顶栏按钮一致的浅色样式，按钮不再被压成竖排文字，窄屏也不隐藏文字标签。
- **输入区精简**：移除「轻量」与「富文本」两个开关——工具与 Skill 的能力范围完全由 Agent 设置（Agent 的工具集 / 技能集）决定，不再有会话级轻量模式；富文本始终开启（安全白名单 HTML 正常渲染）。同时移除会话界面中「本次对话规则：未设置」提示条（会话规则已统一由 Agent 的系统提示词决定）。
- **会话收藏与侧栏交互**：会话条目的齿轮改为**五角星**，点击即收藏/取消收藏（乐观更新，失败回滚）；侧栏最下方新增跨工作区的「已收藏」分组，收藏的会话**同时保留在原工作区**（两处都显示，不会"消失"），且**收藏不会重排工作区内的会话顺序**（收藏只改标记、不推进更新时间，避免收藏项被顶到最前、启动时"最新 5 条"被挤占）；重命名与删除并入条目的「⋯」菜单（fixed 挂到 body 不被侧栏裁剪，Esc 或点击外部关闭），**新建工作区也改为对话框**（选目录 → 填名称，不再依赖内嵌浏览器的原生 prompt）；顶栏重复的「新会话」按钮已删除（每个工作区分组自带「＋ 新会话」，列表为空时保留兜底入口），**收起按钮并入「工作区」操作行**（不再单独占一行）；**点击会话不再把列表强制滚到最上**——已可见就保持当前位置、不可见才用最小位移带进视口；**会话条目整条可点**（含上下边缘与左侧留白，鼠标落上去即变手型）；侧栏底部只保留**一个状态点**（绿=服务已建立、红=连不上服务端，随每次请求自动刷新）；**收起侧栏只重新居中对话列**（列宽恒定 880px，不再放大到整屏——此前收起后助手区块左移、用户气泡右移、整体错开）；条目更紧凑（高度 40→34px、标题 13→12.5px、时间 11→10px、行内间距 6→4px）；**滚轮步进按 1.5 倍接管**（≈150px/格，触控板小步进仍走原生），并修掉三处手感问题——虚拟列表的滚动上限此前漏算容器 padding（每次滚动被回写吃掉 16px，"越滚越慢"、底部到不了）、滚动时每帧重写 DOM（改为只在可视行区间变化时重绘，实测 160px 滚动的重绘次数从 20 降到 ≤8）、悬浮分组表头时删除按钮撑高行（改为绝对定位 + visibility，划过不再抖动）。
- **会话级系统提示词退役**：系统提示词**只有一个来源——Agent**（`agent.system_prompt`，为空时回退全局默认）；顶栏「对话设置」按钮、会话条目齿轮入口与整个对话框（含流式输出开关、清空对话）一并移除，`conversations.system_prompt` 作为遗留列保留但**不再注入模型**（历史值仍可在「首轮上下文」卡片里看到当时原文）；角色卡导入与快捷提示词套用统一迁到**设置 → Agent → 编辑**的「系统提示词（预设与规则）」块（标题行右侧即工具栏：套用快捷提示词 / 存为快捷提示词 / 导入角色卡 PNG / 自定义头像；角色卡为**追加**语义，不覆盖手写内容；超 12000 字符截断并提示）；快捷提示词改为**在 Agent 表单里管理**——「存为快捷提示词」把当前文本框另存（弹窗可改标题与正文）、面板里点条目套用、✎ 编辑、× 删除（**设置页的「快捷提示词」页已整体下线**）。

### 历史能力（1.7.8 及更早）

- **对话快捷系统提示词**：新增「设置→对话快捷系统提示词」，可新增/编辑/删除/搜索可复用的系统提示词预设；对话设置中可直接选择预设一键填充系统提示词，编辑预设不会改动已保存的对话。
- **角色卡自动收录**：导入 SillyTavern 角色卡 PNG 解析成功后，自动把该角色的人设文本收录为一条快捷系统提示词，无需手动复制。
- **更新检查自动选中新版本**：检查更新完成后自动在下拉框中选中新版本，无需展开下拉框即可看到有更新；手动选择版本后仍按用户选择保留。

### 历史能力（1.7.7 及更早）

- **文件预览隔离**：右侧文件面板的 Markdown 预览改用独立的 `markdownFilePreview` 渲染入口，固定按纯文本/Markdown 渲染，不受对话渲染开关影响，逻辑更清晰。
- **渲染性能优化**：内容不含任何 HTML 标签的消息在富文本还原阶段直接原样返回，跳过正则解析，降低大段纯文本回复的渲染开销。

### 历史能力（1.7.6 及更早）

- **对话富文本渲染**：AI 回复中的安全白名单 HTML 富文本（`b`/`i`/`u`/`s`/`span` 与 `font color` 彩色文字）渲染为实际效果；渲染前先整体 HTML 转义、仅对白名单标签按安全规则还原，颜色只允许标准颜色名或十六进制色值，注释一律剔除，其余标签保持纯文本显示，杜绝注入。右侧文件面板中的 Markdown 预览固定按纯文本渲染。
- **文件面板可拖宽**：右侧文件面板新增左缘拖拽分隔条，宽度可在 280px 至窗口一半之间自由调整并记忆（重启保留），不再固定；图片预览取消 58vh 高度上限、改为面板内纵向滚动，可完整查看高图。

- **Job 查询与状态提示分级**：跨会话查询 Job 时区分「记录已被清理」与「Job ID 从未存在」；job_status 按状态给出下一步指引（completed 用 job_output 读结果、运行中用 job_wait 等待等）；用户消息含明确 Job ID 时直接验证、不先检索历史。
- **SillyTavern 角色卡解析增强**：parse_sillytavern_card 对 chara 负载先尝试 gzip 解码并兼容 data 字段包裹，支持更多角色卡导出格式。

- **SillyTavern 角色卡导入**：对话设置中新增「导入角色卡 PNG」，由服务端解析 PNG 内嵌的 ccv3/chara 元数据（兼容 gzip 压缩），把姓名、性格、背景/场景、说话示例、开场白、角色说明、后置指令等字段归一化为系统提示词人设文本，一键填入即可扮演该角色；当前 Agent 自带系统提示词时会提示建议切换到提示词留空的 Agent。
- **细节优化**：会话列表的对话设置按钮在未悬停时改为半透明可见，不再完全隐藏，更易发现。

- **工作区名称绑定**：会话所属工作区（分组）与磁盘目录改由服务端统一绑定——指定已注册工作区时目录一律取其注册路径，不再信任客户端随分组提交的目录；分组名不存在会直接拒绝并给出明确提示。
- **切换分组即切换目录**：把会话切换到某个工作区分组时，该会话的磁盘工作区同步指向该分组注册目录；「移至未分组」只改侧栏分类、保留原有目录，不会移动文件。
- **启动自动修复**：启动时检查全部会话，凡分组指向已注册工作区但目录与注册值不一致的自动改回注册目录；未分组或指向已删除分组的会话保持原样，避免误搬文件。
- **状态同步**：切换工作区后前端立即以服务端返回的目录刷新界面，侧栏分组、底部提示与磁盘目录保持一致。
- **代码块语法高亮**：聊天中的代码块按语言实时着色（支持 JS/TS、Python、JSON、Bash/Shell、YAML、CSS、HTML，语言别名自动归一化，如 `js`/`javascript`/`jsx`、`py`/`python`、`sh`/`bash`/`ps1`），关键字/字符串/数字/函数名/注释分色渲染；流式输出随内容增长实时高亮，不再等整段结束才着色。
- **UI 图标 SVG 化**：设置、发送/停止、刷新、卸载模型、删除/编辑/关闭、对话设置、快捷指令等按钮图标从 emoji/字符替换为内联 SVG，手机与桌面渲染一致清晰，不再出现 emoji 字体缺失导致的方块或乱码。
- **顶栏「更多」菜单**：卸载模型、刷新页面等低频操作收进顶栏 ⋯ 溢出菜单，主操作区更简洁；菜单带 aria 支持，点击菜单外部自动关闭。
- **快捷指令卡片化**：空状态快捷指令改为「图标+标题+说明」卡片布局（通过 MCP/HTTP 调用 ComfyUI、设置本地 Comfy MCP、列出工具/文件等），按钮标题精简、说明行补充用途，并适配窄屏。
- **聊天布局调整**：AI 消息外层新增消息卡片结构，样式与代码高亮配色统一。
- **工具精简**：移除 `fetch_page`（抓取并提炼网页正文）工具，Agent 编辑页工具目录、内置 Agent 全量工具范围与权限确认清单同步移除；需要读取网页内容时改用 `http_request` 或已配置的 MCP 抓取能力。

- **本轮修改文件**：AI 每轮用 `write_file`/`edit_file` 成功写入的文件会在消息末尾按「新建/编辑」列出摘要（图片/视频/音频等多媒体产物仍走原附件卡片预览），点文件名在右侧文件面板打开，随时回看 AI 改过什么；中止/失败轮次已写入的文件同样留痕。
- **右侧文件面板**：多标签页管理打开的文件；Markdown 直接渲染预览，文本文件可手动编辑并保存回磁盘（Ctrl/⌘+S 或 Ctrl/⌘+Enter 保存、Esc 取消）；图片直接预览并可点开大图，二进制文件给出明确提示。
- **文件访问边界**：面板只允许查看本会话改动过或位于会话工作区内的文件，「保存写回」要求同时满足两者，路径越界一律拒绝，不能当作任意文件读写入口。
- **侧栏折叠**：左侧会话栏支持折叠与展开（桌面端记忆状态、腾出聊天区）；右侧文件面板收起后可从顶栏「文件 N」按钮一键重新打开已保留的标签。

- **检查更新更稳**：启动时复用 6 小时内的版本列表缓存，减少对 GitHub API 配额的无谓消耗；列表接口失败（403/限流/网络异常）时自动回退读取本地缓存，不再因一次接口失败就报错。
- **latest 静态直连兜底**：列表接口不可用或所选版本清单拉取失败时，自动改用 GitHub `releases/latest/download` 静态清单判断是否有新版本（不占 API 配额），并合成可安装条目，兜底场景下仍可一键安装。
- **错误提示分级**：GitHub 接口限流（403/429）时提示「访问频率受限，请稍后重试」；其它 403 提示检查网络或代理；网络无法连接给出明确提示。

- **删除会话不中断当前输出**：删除非当前会话时只刷新侧栏列表，不再自动重载当前会话视图，正在流式输出的消息行保持原样继续渲染。
- **流式输出稳定**：修复之前删除任意会话后自动重载当前会话，导致进行中的流式消息行被挤出 DOM、输出看似消失、要等 run 结束重新渲染才重现的问题；现在只有删除当前会话才切换到下一个会话。

- **选项识别收紧**：只认明确的「请选择/选一个/choose from」等完整意图，正文或代码里的裸 `select`/`choose`/`pick` 不再误判成选项按钮；英文只保留完整词组（`pick one`/`choose one`/`select one`/`which one`/`which of`/`choose from`/`select from`）。
- **意图必须紧跟选项**：意图行与选项组之间最多允许少量空行/单行间隔，长距离的「选择」字样不再触发按钮；选项文本过长（整段话）不算交互选项，意图行过长（长句里恰好含「选择」）也不算意图。
- **紧凑选项正确拆分**：「请选择语言：1. 中文 2. 英文」这类同一行紧凑选项可正确拆分为多个选项按钮，CJK 标点后的编号也能识别。
- **编号残留剥离**：选项中的「- 1. 安装依赖」会剥离编号残留，只保留「安装依赖」；孤立的 bullet 符号（`- `/`• `）不再打断正在收集的选项组。
- **点击选项只发选项文本**：点选按钮发送时只发送该选项本身，不再拼接「请选择…」前缀，避免把问题重复带进下一条消息。

- **命令执行统一**：`run_command` 已并入 `pwsh`，命令执行只保留 `pwsh` 一个工具，避免重复声明与选择困惑；升级后旧配置、Agent 工具范围与历史会话中的 `run_command` 会自动迁移为 `pwsh`，内置/自定义 Agent 均不会丢失命令执行能力。
- **手机端渲染优化**：修复移动端横向滚动条问题（视口宽度改用 100% 并隐藏横向溢出）；顶部模型/Agent 选择器改为网格布局自动换行，操作按钮在窄屏下换行到第二行，超小屏（≤380px）降级为两列；输入框在窄屏下完整占宽，图标与发送按钮不再被压缩；底部工作区、权限开关等元信息在超小屏改为单列排列。

- **暂停/取消竞态修复**：一次暂停立即中断当前事件流、看门狗与待执行重连，并停止轮询恢复；「正在停止」期间禁止发送和恢复旧 Run，服务端确认终态后才恢复；取消父 Run 会级联取消子 Job 与 follow-up Run；完成/暂停并发窗口加锁，取消后不再创建自动 follow-up。
- **断线重连稳定**：重连复用同一回答气泡并从最后事件序号继续，不再重复回放；`/api/chat/cancel` 支持通过 `conversation_id` 定位尚未返回 `run_id` 的 Run；未处理插话保留在记录中并标记为停止，不会自动发送；Job 即使在轮询返回的竞态中完成，也会服从已有取消标记。
- **RunningHub 多媒体生成**：内置 runninghub 技能，覆盖文生图、图生视频、文生视频、TTS、音乐、3D 建模、图片放大、AI 应用（webappId）等 420+ 端点；支持 AI 站（runninghub.ai）与 CN 站（runninghub.cn）双站点，技能随 EXE 打包，升级后启动时自动同步到本地数据目录。
- **Skill 脚本执行稳定**：安装版（冻结 exe）执行 .py Skill 脚本不再二次启动主程序触发实例锁；冻结版走 `--run-skill-script` 隐藏入口只执行脚本，`__main__` 语义、工作目录与参数透传保持不变。
- **失败内容保留**：在线请求异常（如 HTTP 500）时已输出的思考/部分回复/工具活动不会被错误信息覆盖丢失，会重建为「未完成」assistant 消息持久化并保留独立错误提示，刷新/重渲染/后续上下文可见。
- **启动错误清晰**：数据目录/锁文件不可写等权限/环境问题不再误报为「已在运行」，会给出具体目录与原因；启动失败以简洁中文提示退出，不再输出堆栈。

- **多轮图片缓存稳定**：图片对话历史按每条 user 消息完整携带并稳定重发，不再因「只保留最近图片/占位翻转」导致 DeepSeek 缓存命中率波动；图片以 `image_url`/`input_image` 内容块发送，命中率随对话稳定上涨。
- **切换 Agent 附加 Skill 引用**：切换顶部 Agent 后自动在输入框末尾追加该 Agent 预设 Skill 的 `/ref` 引用（已在框内的跳过，避免重复）。
- **Skill 系统重构**：输入 `/` 弹出已安装 Skill 索引，可在任意时刻向当前会话注入任意 Skill（`/ref` 引用）；引用在首轮注入 system、后续轮追加到会话末尾，保持 DeepSeek 前缀缓存稳定。
- **安装/编辑 Skill**：新增「安装 Skill」「编辑 Skill」按钮，AI 可调用 `install_skill`/`unpack_skill_archive` 安装技能，或用 `inspect_installed_skill` 定位并编辑已有技能。
- **左侧边栏改进**：会话列表虚拟化/懒加载、默认折叠、可拖拽调宽，交互更轻量清晰。
- **意图直接触发工具**：工具不再依赖先启动某个 Skill。Skill 只提供领域说明和流程建议，不是能力开关。
- **通用 Agent 自动化**：支持多步工具调用、后台 Job、状态查询、计划执行、子任务和失败后的有界恢复。
- **会话工具集固化**：会话可用工具在首条消息时固化（`enabled_tool_ids` / Agent 的 `tool_scope`），之后不可中途更改；工具选择页支持按分类「全选/全不选」，联动工具须同时选中。
- **更少的中断确认**：读取、检查和常规工作区操作可按权限模式自动执行；只有高风险操作、缺少凭据或不可推断的关键选择才需要用户确认。
- **流式渲染稳定**：代码块等特殊显示框随内容增长实时渲染在框内，不再在流式阶段被当作纯文本、等整段回复输出完才突然变成代码框；含大括号/方括号的代码块（如 Java/C/JS 的 `{ }`、`[Shot 1]` 标签）不再被误判为工具调用而中断流式输出，多个代码块可随内容同步渲染；Markdown 表格（`| 列 | 列 |` + `| --- | --- |`）可正确渲染——流式时表头+分隔行一到即渲染成表格、数据行逐行增长，单元格支持加粗/内联代码/链接，含 `|` 的内联代码与 `\|` 转义竖线都不会被切碎；长答复不再被截断到 2000 字符；`**a * b**` 这类含字面星号的加粗可正确渲染；思考块与工具块按时间顺序交错显示，正文不再被清空吞掉。
- **ComfyUI 自动任务**：可检查 API 工作流、修正运行参数、批量提交、轮询队列并收集图片、视频和音频产物。工作流推荐「改本地文件、再引用」：用 `read_file`/`comfyui_prepare_workflow` 读取、`edit_file` 局部修改，再用 `comfyui_batch` 的 `workflow_paths` 提交文件路径，避免把大工作流 JSON 内联进对话。
- **宿主管理产物**：生成结果由 Naiba Chat 宿主下载、校验、保存到受管理数据目录并附加到消息；即使中止/取消的轮次也会展示已生成的图片缩略图。用户明确要求「把某张图保存/复制到指定目录」时，模型会实际执行以满足该要求。
- **图片与视频预览**：聊天内可直接预览常见图片和视频格式，也支持音频播放与文件下载；生成产物会自动缓存并生成缩略图（含内容级去重，避免同图反复显示）。
- **多模态路由**：原生视觉模型可直接接收图片；文本模型由模型按需调用 `vision_analyze` 工具（独立视觉后端）看图，结果与其它工具一样以工具块呈现。
- **在线与本地模型**：支持 OpenAI-compatible、Anthropic、Gemini、Ollama、LM Studio、llama.cpp、Unsloth 等常见接口；针对 DeepSeek 思考模式会正确回传 `reasoning_content`。
- **网络健壮性**：针对代理/VPN TUN 中途切换或代理进程重启导致的连接拒绝，会自动改用直连（绕过系统代理）重试；修复取消请求时的连接泄漏；错误提示附带完整接口路径便于定位。
- **Skill 与 MCP 扩展**：Skill 可提供领域知识、模板和脚本。MCP 依赖升级到 2.1.1，并在应用启动时自动连接已启用服务（对启动时未连上的做周期重试）；`register_mcp` 只是登记服务，需重开会话后其工具才进入可用集，`call_mcp` 仅能调用当前会话可用集内的 MCP 工具。
- **可校验更新**：更新检查基于 GitHub Releases 版本列表，支持在设置中选择版本安装与回退；每个候选版本都会校验 manifest（仓库、commit、文件名、SHA-256）与可执行文件有效性，任何一项不一致都会拒绝安装并给出明确原因。

## 开始使用

### 使用 Windows 版本

1. 下载 `naiba-chat-2.3.7-beta-windows-x64.zip`。
2. 解压到一个可写目录。
3. 运行 `naiba-chat.exe`。
4. 在设置中添加在线 API 或本地模型服务。

首次运行会创建本地数据目录。升级时请直接替换程序文件，不要删除原有 `data` 目录和配置文件。

### 从源码运行

需要 Windows、Python 3.11 或更高版本。

```powershell
git clone https://github.com/yc883123/naiba-chat.git
Set-Location naiba-chat
python -m pip install pywebview pystray pillow "mcp==2.1.1" pymupdf python-multipart
python server.py
```

服务默认地址为 `http://127.0.0.1:8765`。

## 模型配置

在“设置 → 模型”中添加供应商，填写：

- API 地址
- API Key（本地服务通常可以留空）
- 模型名称
- 请求格式
- 是否支持图片输入
- 上下文长度和生成参数

本地模型建议先使用界面中的连接测试确认模型目录、文本推理和图片能力。模型是否能稳定执行工具，仍取决于模型自身的工具调用能力和上下文能力。

## ComfyUI 与 MCP

Naiba Chat 默认连接：

```text
ComfyUI HTTP API:  http://127.0.0.1:8188
```

使用前请确保 ComfyUI 已启动，并且目标节点、模型和素材在 ComfyUI 中可用。

当前内置流程接收 ComfyUI **API 工作流**。前端保存的 UI JSON（通常包含 `nodes` 和 `links`）不能直接提交，需要先在 ComfyUI 中导出 API 格式。

任务提交后由宿主完成以下工作：

1. 校验并规范化工作流参数。
2. 向 `/prompt` 提交任务。
3. 轮询任务历史和执行状态。
4. 自动收集图片、视频或音频产物，缓存到受管理数据目录并生成缩略图。
5. 附加到最终消息（中止/取消的轮次也会展示已生成的产物）。
6. 在聊天界面提供预览或下载。

推荐的工作流修改方式：对已有工作流文件，用 `read_file`/`comfyui_prepare_workflow` 读取结构，用 `edit_file` 做局部精确替换（改提示词/seed/尺寸等），再用 `comfyui_batch` 的 `workflow_paths` 提交文件路径；不要整段内联大工作流 JSON。

这条链路不要求模型读取产物本身。需要评价画面、OCR、定位对象或比较图片时，用户应明确提出分析要求；用户明确要求「保存/复制到指定目录」时模型会实际执行。

### MCP 说明

- 应用启动时自动连接所有已启用的 MCP 服务，并对启动时未连上的服务做周期重试。
- MCP 工具以 `mcp__<server>__<tool>` 形式注册进当前会话的可用工具集；会话工具集在首条消息时固化。
- `call_mcp(server, tool, arguments)` 只能调用**当前会话可用集内**的 MCP 工具；若目标工具不在可用集内，会提示用户重开会话、在 Agent 工具勾选里加上该 MCP 服务后再用。
- `register_mcp` 只是把服务登记进配置，其工具会在**重开会话后**进入新会话的可用集，本会话内不会因注册而新增可用工具。

## 工具、Skill 与 MCP 的关系

- **内置工具**：由当前任务意图直接触发，例如文件操作、命令执行、后台任务、ComfyUI 和视觉处理。
- **Skill**：提供特定领域的说明、模板、脚本和检查清单，不决定工具是否可用。
- **MCP**：接入外部工具服务；只有任务明确需要对应 MCP 服务时才会连接或调用。

因此，生成图片不需要先启动短剧 Skill，普通编程、资料整理、自动化处理和媒体任务也使用同一套运行机制。

## 数据与安全

- 对话、设置、附件和任务状态保存在本地数据目录。
- 缓存文件（用户上传与宿主产物的图片/PDF/视频/音频等）统一存于受管数据目录（用户上传 `data/uploads`、生成产物 `data/generated`），设置页可查看合计大小、按时间清理旧缓存（清理为手动操作，会按时间从旧到新删除最旧文件、不区分是否被引用）并配置自动清理阈值（默认 256MB，自动清理只删未被消息引用的最旧文件）。
- API Key 不应提交到 Git；示例配置不包含真实凭据。
- 局域网访问需要访问口令，默认仅本机访问时不要求口令。
- 文件预览只允许应用工作区和受管理数据目录（`data/uploads`、`data/generated`）中的文件。
- 高风险写入、删除、外部发布和凭据操作仍受权限策略保护。

## 自动更新校验

发布资产包含：

- `naiba-chat.exe`
- `naiba-chat-update.json`
- `naiba-chat-2.3.7-beta-windows-x64.zip`

更新器会验证清单中的仓库、提交、文件名和 SHA-256。下载文件还必须是有效的 Windows 可执行文件；任何一项不一致都会终止安装。

## Beta 说明

这是 2.3.7 Beta，适合实际使用和反馈，但仍有以下边界：

- 不内置 ComfyUI、模型权重或第三方生成服务，需用户自行安装和配置。
- 不同模型的工具调用质量差异较大，小型模型可能无法稳定完成长链任务。
- ComfyUI 自定义节点和工作流输入差异很大，复杂模板仍可能需要一次参数适配。
- 自动化任务应在交付前检查最终文件；涉及发布、删除或覆盖的重要操作应保留备份。
- MCP 工具仅在重开会话后才会进入会话可用集；若模型发现 MCP 服务存在但其工具不在当前会话可用范围，会提示用户重开会话。

## 测试与构建

```powershell
Get-ChildItem public\js\*.js | ForEach-Object { node --check $_.FullName }
python -m unittest discover -s tests -q
$env:NAIBA_BUILD_VERSION = "2.3.7-beta"
python -m PyInstaller --noconfirm --clean naiba-chat.spec
```

静态检查器、浏览器冒烟与数据播种脚本**随仓库发布**在 `verify/`（完整清单与用法见 `项目维护说明（修改代码前必读）.md` §六）：

```powershell
python verify\scan_undef_all.py        # 未定义名扫描（Python 全包）
python verify\esm_graph_check.py       # 前端 ESM 导入/导出图一致性
python verify\tdz_check.py             # 前端顶层求值序（TDZ）候选
node verify\media_markup_check.mjs     # 媒体渲染（无浏览器）
$env:NODE_PATH="%USERPROFILE%\node_modules"
node verify\browser_smoke.cjs          # 浏览器冒烟（需 playwright + Edge）
python verify\ring_usage_smoke.py      # 上下文圆环/提醒端到端冒烟（自编排）
```

浏览器冒烟需要本机 `npm install playwright` 并已安装 Edge；脚本一律用相对项目根的路径，换机器无需改代码。

## 项目地址

- GitHub：<https://github.com/yc883123/naiba-chat>
- Issues：<https://github.com/yc883123/naiba-chat/issues>

