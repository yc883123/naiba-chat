Naiba Chat Build 81

This package contains the Windows x64 executable for Build 81.

Highlights:
- llama.cpp, Ollama, and LM Studio image capabilities are detected from live runtime metadata, with a real-image fallback probe when needed.
- API provider management is separated into clear Online API and Local API tabs without changing the visual-provider selector.
- Newly installed Skills refresh in conversations immediately, and references folders no longer appear as separate Skills.
- The built-in ComfyUI模型与Llama模型互通 Skill shares GGUF/mmproj guidance, measured runtime recommendations, and a read-only compatibility inspector.
- Model profiles can explicitly choose automatic detection, image support, or text-only behavior.

The automatic updater verifies naiba-chat.exe against the SHA-256 value in
naiba-chat-update.json before installing it.
